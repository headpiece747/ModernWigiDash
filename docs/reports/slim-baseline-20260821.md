# Slim-Down Program — P0 Baseline (2026-08-21)

Program: `docs/superpowers/plans/2026-08-21-global-usings-slim-modern.md`.
This report is the P0 evidence base. Sub-reports:
- `docs/reports/slim-baseline-health-20260821.md` — 8-dimension health check (A-, GPA 3.75)
- `docs/reports/slim-baseline-packages-20260821.md` — package freshness sweep

All numbers below were re-verified in the main session with Glider after the
subagent runs (blast-radius rule: a "nothing to do" verdict needs its own
evidence).

## Health (summary)

| Dimension | Grade | Note |
|---|---|---|
| Build | A | 0 errors / 0 warnings; `get_diagnostics` (warning+) = 0 diagnostics |
| Code quality | A | No high-confidence antipatterns after triage |
| Architecture | A | Inward dependencies, 0 cycles, 0/13 unused project references |
| Test coverage | B | 1610 test methods; 42% structural name-match floor; no line-coverage evidence |
| Dead code | A | 0 unused production symbols |
| API surface | A | Bounded exposure; public surface is deliberate |
| Security | A | 0 vulnerable packages, no hardcoded secrets |
| Documentation | B | XML docs emitted by Sdk only; 4 projects lack emitted docs |

## Dead code (P2 input)

| Check | Result | Action |
|---|---|---|
| `find_unused_symbols` (Private,Internal, whole solution) | 2 candidates — both `AutoGeneratedProgram`/`Main` from the Microsoft.NET.Test.Sdk NuGet package's own generated file, not repo code | none — 0 deletable repo symbols |
| `find_unused_parameters` | 42 — all in method signatures whose parameter list is fixed by an event-handler delegate contract (27 MainWindow, 10 WinRt adapters, remainder scattered) | none — signatures cannot change |
| `find_unused_project_references` | 0 of 13 unused (each removal introduces 60–2396 compiler errors) | none |
| `find_package_consolidation_candidates` (conflicts) | 0 conflicts; all 12 CPM packages consistently central-managed | none |
| Package source usage (`find_package_usages` per direct package) | MessagePack, NAudio.Wasapi, ProtectedData, Logging.Abstractions, LibUsbDotNet, SkiaSharp, SkiaSharp.Views.WPF (via XAML SKElement) all have live consumers | none — no zero-usage `PackageReference` |

**P2 verdict:** the only deletion is the approved one-line
`<AssemblyName>` removal in `ModernWigiDash.Hardware.csproj`. No dead symbols,
no dead references, no version drift.

## Package freshness (P3 input)

All 16 CPM pins are at the latest stable version (verified against the NuGet
flatcontainer feed, not memory). `dotnet list package --vulnerable
--include-transitive` clean across all 6 projects. No MediatR/MassTransit/
FluentAssertions/AutoMapper license traps.

Caveat recorded in the sub-report: the NAudio.Wasapi feed lists an unlisted
22.0.0 (hidden 2023 legacy mis-versioned publish) — the true latest is the
pinned 3.0.1. Do not "upgrade" to 22.0.0.

**P3 package-upgrade leg: no-op.**

## C# 14 / shape scan (P3 input)

| Candidate class | Count | Verdict |
|---|---|---|
| `Span`/`Memory`-typed method parameters (`scoped` candidates) | 0 | no-op — the frame pipeline is byte[]/pool-based by design (the pool is sized from the encoder output; arrays are the pooling unit) |
| Collection-literal `new List<T> { … }` / `new Dictionary<T,U> { … }` shapes | 4 (2 in test files) | convert when touched; not a standalone sweep |
| Underscore backing-field lines | 365 | no bulk `field`-keyword conversion — churny, behavior-identical diff across ~40 files; convert opportunistically in files already being edited |
| Primary-constructor gaps | not scanned in P0 | P3: targeted pass with the `modern-csharp` skill as reference |

## Open items (for user decision, not silently executed)

1. **Line-coverage evidence (health finding 2):** add one XPlat Code Coverage
   baseline run as a regression floor. New local dotnet tool (not an MCP);
   not part of the approved program.
2. **XML docs in Core/Hardware/Widgets (health finding 3):** enabling
   `GenerateDocumentationFile` there surfaces missing-doc gaps at build time
   but changes the build-warning surface. Addition, not slimming.
3. **Complexity hotspots (health finding 1):** DialogHost.ShowIconPicker (CC 19),
   InspectorPanelRenderer.Render (CC 19), 10 more at CC ≥ 15. CONTEXT.md
   records these as deliberate and reviewed (MA0051 off) — refactoring them is
   a separate effort, out of scope for slim/modern.

## P1 gate facts (for the usings sweep)

- Per-project global-using candidates: see the plan's table (≥5 files per
  assembly).
- 48 file usings restate the `ImplicitUsings` baseline and are deleted by the
  same sweep.
- 5 alias usings and 0 `using static` lines — the sweep exact-matches plain
  `using <Ns>;` lines only.
- Commit shape: one commit per project (6), build + full tests + format green
  each.
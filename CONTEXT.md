# ModernWigiDash: Domain Glossary & Architecture

## What this project is

ModernWigiDash is a .NET 10 WPF application that drives a USB-connected small LCD display (G.Skill WigiDash, VID 0x28DA PID 0xEF01, 1016×592 pixels, RGB565 framebuffer). The application composites customizable widgets (clocks, weather, hardware telemetry, Twitch chat, stock tickers, etc.) onto pages and streams the rendered frames to the display over USB HID/WinUSB.

## Domain Glossary

### Core Concepts

| Term | Definition |
|------|-----------|
| **WigiDash display** | The physical 1016×592 RGB565 LCD connected via USB HID (WinUSB or LibUsbDotNet). Manufactured by G.Skill. |
| **Widget** | A self-contained UI component displayed on the WigiDash canvas. Each widget has a metadata attribute (`[WidgetMetadata]`), renders via SkiaSharp to an `SKCanvas`, and receives touch input. Widgets are registered by reflection and instantiated by `WidgetPluginLoader`. |
| **Page** | A layout container holding placed widgets. The profile holds ordered pages; users swipe between pages at runtime. |
| **PlacedWidgetInstance** | A widget bound to a position on a page, holds coordinates, size, rotation, opacity, and the widget instance. |
| **Profile** | The persisted set of pages, active page index, and widget placements. The page-range invariant is enforced at the type: the `Pages` setter repairs a null/empty assignment to a default page and the active index is clamped on write (re-clamped on read), so `ActivePage` is total. The old orphan-page fallback (a detached page whose mutations were silently lost) is unrepresentable. |
| **Frame** | A pixel buffer (SKBitmap) composited by `SkiaFrameCompositor`, converted to RGB565, and streamed to the display over USB. |
| **FrameSink** | ~~A destination for composited frames: `IFrameSink` exposes `SendFrame(SKBitmap)` + `IsReady`, returning a truthful `FrameDeliveryResult`.~~ **Removed (ADR-0005)**: the WCF sink and `FrameSinkRouter` were removed with the Service. The App binds one `FrameDelivery` instance to the direct-USB engine (see the `FrameDelivery` row). |
| **FrameDelivery** | The single frame-delivery policy module (Sdk): bounded DropOldest channel → drain-to-latest → paced send, owning encode, an exact-size buffer pool sized from the encoder's output (self-sized by construction, a pool that disagrees with the encoder is unrepresentable), and drop accounting. The send seam returns the truthful `FrameSendResult` (Sent / Refused / Failed), so the delivery's accounting splits a device refusal (`SendRefusedCount`, no live connection, or the frame fails the transport's size contract) from a broken pipe (`SendFailedCount`). The two no longer share one bool, and both are distinct from drops. One entry point feeds the policy: `Push(SKBitmap)` (encode + pool, void: the drop verdict is observable through the counters, not returned per call); a `FrameDelivery.Create` factory makes the required encode/pool/send seams unrepresentable at production bind sites (the ctor remains for tests exercising unconfigured readiness). The App's direct-USB sink is its one runtime instance; pacing defaults to 33ms, the USB engine's device-capability rate. Drop counting lives inside the module, split by reason (`DroppedUnconfiguredCount`, `DroppedNotReadyCount`, `DroppedPoolCount`, `DroppedCoalescedCount`, `DroppedEncodeCount`) under the `DroppedCount` total, and the drop verdict is observable: pool-exhaustion, coalesced-backlog, and post-dispose drops log through the same injected seam at first-plus-every-60th cadence (the production bind site passes its FileLog sink), so a wedged pipe that drops frames is visible in the display log. The encode runs on the caller thread inside `Push` (the compositor owns exactly one SKBitmap, so the sender cannot encode a bitmap the compositor has already moved on to). A compositor-side bitmap ring was evaluated and declined this pass (2026-08-24 hot-path triage): it would need a depth at or above the channel capacity plus the sender slot, at ~1.2 MB of pixels per slot, and the sender would encode a bitmap that is not the one the canvas draws, breaking the buffer-drawn-is-exactly-the-buffer-sent identity; the encode's dispatcher cost is pinned instead by the `FrameEncoderPixelTests` timing canary (a full-frame encode under a generous bound), so a regression on the tick's thread fails a test rather than showing up as frame hitches. |
| **DisplayGeometry** | The WigiDash framebuffer geometry (Sdk), the single source of truth for the active pixel area (1016×592, 2 bytes/pixel, payload size). Hardware's `DisplayProtocolConstants` aliases these values and Core's compositor derives its buffer from them, so the pixel area can never drift between projects. |
| **FontCacheEviction** | The font-memoization overflow-bound rule (Core/Rendering): every FontHelper cache keyed by content variety (glyph presence, fallback typeface, text-run splits, cached fonts, per-font meta) is bounded by the same coarse reset, strictly over its own declared cap, clear the whole cache and let it refill on demand (no LRU; every entry recomputes, so an evicted entry costs one recompute). Each cache's cap is declared exactly once in the module (`GlyphPresence`/`FallbackTypeface`/`TextRuns`/`CachedFont`/`FontMeta` limits), so the five bounded caches can no longer drift from one another or from the tested boundary (`FontCacheEvictionTests` pins the exclusive greater-than boundary and the whole-reset eviction). The interop `SKFontStyle`/`SKTypeface` wrappers are fresh objects on every property read with no value equality, so the style is keyed by its weight/width/slant value and the typeface by its native handle; the per-font meta cache resolves the interop `SKFont.Typeface`/`FontStyle` reads once per font instance, so the draw/measure path pays zero wrapper churn per tick (`FramePipelineAllocationTests` pins the per-tick allocation budget). |
| **IRgb565Encoder / SkiaRgb565Encoder** | The encode seam (Sdk/Hardware): `IRgb565Encoder` exposes `OutputBufferSize` + `Encode(SKBitmap, byte[]). The delivery pipeline encodes through it, and its output size sizes the delivery's buffer pool; `SkiaRgb565Encoder` is the production adapter over `FrameEncoder`. Tests inject scriptable fakes, so frame delivery is testable without pixels. |
| **ConnectProvider** | One connect attempt in `DisplayHidTransport.Connect`'s provider loop (Hardware): `TryCreate` opens the device and returns the backend (or null when that driver stack cannot provide one). The record carries only the leg's identity, the diagnostic `Tag` (the file-log vocabulary the loop and the leg bind into their `DiagLog` instances, so no line hand-bakes its tag), the display name, and the leg's success line; the loop spells its own position-aware failure lines from the tag, so vocabulary cannot drift between provider and leg. The default list holds the WinUSB provider first and the LibUsbDotNet fallback second; tests inject fakes through the single `ProviderFactories` seam, including a fake LibUsb leg, to drive the connect fallback policy without hardware. The LibUsb leg's device lookup is itself injectable (`LibUsbDeviceProvider`), so its open/config/claim/endpoint teardown is scriptable; every leg failure disposes the LOCAL device, never the adopted global backend (the orphan rule). The connect path's diagnostic vocabulary (leg steps, init sequence, backend failure lines, the engine's connect machine) is one `DiagLog`-bound tag per step/area. No call site composes its own `[TAG]` prefix. |
| **WinUsbApi** | The WinUSB/SetupAPI P/Invoke surface as an injectable delegate bag (Hardware, the PresentMonNative precedent): production binds the real externs once via `WinUsbApi.Default`; tests inject managed fakes, so `WinUsbBulkDevice.Open`'s failure and cleanup paths are scriptable without hardware. The 11 externs' entry points are spelled explicitly and probed against the real DLLs by `PInvokeBindingTests` (ADR-0020). |
| **Render tick** | The 30 FPS `DispatcherTimer` in MainWindow that calls `Compositor.Compose()`, converts the frame to RGB565, and queues it for delivery. |

### Hardware / Transport

| Term | Definition |
|------|-----------|
| **Transport** | The USB HID communication layer. `DisplayHidTransport` picks one **ITransferBackend** in `Connect` through the **ConnectProvider** loop, WinUSB (primary) or LibUsbDotNet (fallback), and runs its connect/init/frame/touch policy against that seam; the init verdict covers the whole sequence including the blank-frame bulk write (on-device, a post-drop reconnect timed out the 1.2 MB init write at the pipe bound while every control write still succeeded. A connected verdict for such a pipe would inherit the falsehood into the engine, so the failed write fails the leg into the existing fallback → Simulated → 5 s reconnect machinery); the backends own the device-specific transfers and teardown (WinUsbBulkDevice implements the seam directly, LibUsbTransferBackend wraps the chunked-write path); each backend also owns its acquisition sequence (WinUSB's `WinUsbBulkDevice.Open`, the LibUsb `TryOpen`: find → open → config → claim → endpoint discovery → writer), so the transport's connect legs are thin binds into the provider loop and no driver-stack ownership lives in the transport. `IDisplayTransport` exposes only the live surface the App drives (Connect, SendFrame, ReadTouch, GoToStandby) and every member has a production caller. The transport's own connection truth stays a concrete-class member for test observability, off the seam (the engine's ConnectionState is the production truth). The transport names the worst-case teardown duration behind the engine's never-stall-on-close invariant as `CloseBound` (the max of the WinUSB bulk pipe timeout and the LibUsb chunked-write worst case over a full frame) and derives the engine's close budgets from it through **CloseBudgetPolicy** (`CloseBudgets`: the smaller of the designed cap and half the bound), so the invariant is a construction fact, not a cross-file pin. All operations are synchronous. No fake async wrappers. |
| **CloseBudgetPolicy** | The close-budget derivation (Hardware): given the transport's worst-case teardown bound, the engine's two bounded close waits as one record. `CloseBudgetPolicy.Create` derives each budget as the smaller of its designed cap (2 s standby / 3 s dispose) and half the bound, so a lowered pipe timeout can never leave a budget at or above the worst case, and the crossover is exact (the cap holds while the bound is at least twice the cap; below that, half the bound). A non-positive bound (degenerate: the transport's derivation is a max of positive pipe timeouts) clamps to a zero bound with zero budgets, so a degenerate input is an immediate, observable abandon, never a negative wait `Task.Wait` would throw on. The transport exposes the derivation as `CloseBudgets` and the engine's `StandbyCloseBudget` / `DisposeAbandonBudget` read it, so the never-stall-on-close invariant holds across the seam by construction instead of by a cross-file pin (`CloseBudgetPolicyTests` sweeps the bound, pins the crossover, the non-positive-bound clamp, and engine/transport agreement). |
| **ITransferBackend** | The transport's USB transfer seam (Hardware): vendor control in/out + bulk write. Two real adapters (WinUSB, LibUsbDotNet) plus test fakes, so the transport's policy is drivable without hardware and the backend choice is made once in `Connect` instead of re-decided per call. |
| **ConnectionState** | The engine's single connection truth (Hardware): `Disconnected` / `Connecting` / `Connected` / `Simulated`: one value instead of the old lockstep IsConnected/IsHardwareActive/IsSimulationMode trio callers disagreed on. The presenter gate and the USB badge read the same state. |
| **DisplayDeviceEngine** | The App's hardware engine (Hardware): owns transport connection, standby, and the 16ms touch poll. The constructor is deliberately inert, no connect attempt, no background loops (construction never reaches for hardware). `Start()` begins the initial connect, the 5s reconnect timer, and touch polling, so the window's test ctor hands it a fake-transport-bound engine (the `FakeTransport.InertEngine` seam) and a test-host window's Start, touch poll, reconnect timer, and teardown standby never reach the user's attached display (a `DebtGuardTests` pin keeps a bare window construction from creeping back in). The connect machine is drivable end-to-end through the transport factory (the internal test ctor wires the factory to the injected transport plus an explicit initial ConnectionState, so even a Start()-triggered reconnect routes through it); the touch loop's failure observer logs instead of going silent. Close never stalls: the standby and dispose close paths wait bounded budgets (`StandbyCloseBudget`, `DisposeAbandonBudget`) read from the transport's `CloseBudgets` (the **CloseBudgetPolicy** derivation of its `CloseBound` worst-case teardown), so they stay strictly shorter than the worst case by construction rather than by a cross-file pin, abandoning a hung device's handle at exit rather than freezing on it. The engine's one bounded off-thread wait routine (`RunBounded` → `BoundedWaitVerdict`) owns the 2026-08-21 rule — the verdict is read from the task's `Result` only once `Task.Wait(timeout)` confirms completion (Wait reports completion, not the result, the trap that once turned the verdict line into dead code) — and the session-end standby, the dispose standby, and the dispose-abandon wait all route through it, so the completion-versus-result invariant has one owner and each call site keeps only its own `[STANDBY]`/`[Dispose]` verdict line (the FakeTransport's `GoToStandbyFailure` seam pins the throwing standby's failure line at both entry points). `SendFrameBytes` is the liveness guard (disposed / not connected / no adopted transport / null buffer) that relays the transport's verdict. The frame SIZE contract belongs to the transport alone (the engine forwards what it is handed, so the size rule has one owner). |
| **FramePump** | The 30 FPS presentation cadence module (App): dispatcher timer that composes the active page, pushes the frame into the window's FrameDelivery (the compose gate reads the delivery's `IsSendInFlight` directly), and requests a repaint. The window keeps only the WPF draw, so the buffer drawn is exactly the buffer sent. Badge refreshes ride the tick as an injected callback. The pump does not slow to the device: the ~55ms bulk write cannot fit the 33ms cadence, so the compose gate vetoes compose on most ticks while a write is in flight and the device is fed at its measured ~15–18 FPS ceiling; the repaint and badge still fire on a vetoed tick (the badge stays current, the canvas keeps drawing the last-composed buffer). The accepted 2026-08-24 decision (hot-path triage): the ceiling is documented, the pump keeps its 30 FPS contract, and a ring-based overlap would have to beat that contract before it earns the complexity; the ADR-0012 exit conditions were re-checked 2026-08-27 and no trigger fired (canary green, protocol unchanged, no hitch attributed to the encode; the measured write already sits below twice the cadence, so the binding constraint stays the buffer-drawn-is-buffer-sent identity, the ADR's revisit note). |
| **FrameEncoder** | Converts SKBitmap (RGBA) → RGB565 little-endian byte array for the display's framebuffer format (BGRA/RGBA fast paths + per-pixel fallback, shared `PackRgb565` bit packing; handles scaling when the source bitmap isn't exactly 1016×592). The single bit-packing owner behind the **IRgb565Encoder** seam. |
| **TouchReport** | Hardware touch input from the display: type (Down/Up), X/Y coordinates. Polled by the App's `DisplayDeviceEngine` at 16ms in direct-USB mode and normalized once via the shared `TouchReport.ToEventType` (the single mapping site, also used by the deleted Service seam). The App never sees vendor protocol bytes. |
| **DisplayProtocolConstants** | USB vendor commands, framebuffer dimensions, and protocol constants for the WigiDash hardware. |
| **WinUsbBulkDevice** | Raw WinUSB P/Invoke wrapper for bulk and control transfers. Owned lifecycle (opens SetupAPI handle, initializes WinUSB, manages pipe timeouts). |
| **SystemTelemetryPresentation** | The Hardware Monitor widget's display rules (Widgets): the three unavailable placeholders, the label/unit override resolution, the unknown-mode → Gauge fallback, the clamped invariant-culture hero value, the gauge/bar maximum resolution with its value-derived floor, and the 0..1 progress clamp. The widget's render methods are thin adapters over it. The display is assertable without pixels (its math moved out of the widget with it; the presentation recomputes the value string per Build, so the widget's old per-value format memo is retired). |
| **TrustedUriPolicy** | The shell-open trust rule (Sdk): the composite `IsTwitchAuthorizationUri` (https scheme AND host) is the one gate every shell-open site routes through. A host is trusted for the Twitch device-auth browser open only when it IS the twitch.tv apex or a dot-prefixed subdomain; anything but https is refused. A suffix check would admit attacker-registrable lookalikes (faketwitch.tv). The App's device-auth open and the Widgets' auto-open guard both route through it, so the two sites can never drift. |
| **DisplayFormat** | The display-rules culture contract (Widgets): invariant, zero-aware number formatting (`Fps`/`Ms`/`Pct`/`Count`/`Number`) that every presentation module routes through. One locale contract pinned and tested once instead of per-module interpolation with the ambient culture. |
| **WeatherQueryKey** | The weather cluster's resolution identity (Widgets, ADR-0006): the ONE spelling of "which place was this weather resolved for". `Build(WeatherLocation)` is the only key construction: six identity fields (`LocationType|Location|Latitude|Longitude|CountryCode|LocationMatch`), backslash-escaped, `|`-joined (a separator inside a field can never forge a colliding key; `CustomLabel` is deliberately absent so a label edit never re-fetches); `SameKey` is the only predicate (ordinal, null-safe, case is identity). `KeyPropertyNames` / `InvalidationProperties` / `LocationMatchProperty` declare the field sets once, and `WeatherDisplayState.ResolutionInvalidationProperties` aliases the set so the widget's drift test pins it to the record. Every guard site routes through it: the client's capture window (the named **CaptureWindowGuard**, re-validated at save + return), the cache-load identity stamp, and the fetch flow's location-snapshot key + return→apply live re-check (`WeatherFetchFlow`, ADR-0007, the module captures ONE location snapshot per run, so the key and the fetch resolve from the same record), so the stale-identity invariant is assertable in one place, with no pixels and no fetch mocks (`WeatherQueryKeyTests`). |
| **CaptureWindowGuard** | The ADR-0006 capture window as a named value (Widgets): one start key captured before the fetch await + a live-key source, with `StillCurrent()` (the ADR-0006 predicate against the live key) and `Dropped => !StillCurrent()` the only two verdicts. The client builds one per fetch (live source: the fetch control's `LastLocationQuery`, so the post-save re-validation and the return→apply re-check read the same window) and the fetch flow builds one per run (live source: the host's coerced location), so "is the identity I fetched for still the identity the UI shows" has one predicate at every site instead of a hand-composed compare (`CaptureWindowGuardTests`). |
| **WeatherLocationResolver** | The weather geocoding decision policy (Widgets): candidate ranking/scoring, suffix + diacritic-insensitive matching, population tiebreak, the ambiguity gate, alias tables, postal routing (`TryPostalRoute`: numeric 2–10-digit or hyphenated n-n queries read as postal, US-default route unless a country hint is present; alphanumeric codes REQUIRE a hint. A US-guess for an un-hinted foreign code is a coin flip; `NormalizePostalLookup` keeps the indexed shape: US ZIP+4 → 5 digits, GB/CA/IM/je/gg full codes → 3-char short forms; the geocoder search runs the geocoder's maximum `count=100`. The default top-10 hid the user's city from the pick list), the response-aware weak-ISO fallback (a code match earns the weak bonus only when the state candidate is absent from the response), the ONE exact-name predicate (`IsExactNameMatch`: the pick list offers only candidates that BEAR THE NAME THE QUERY TYPED, and a persisted Location Match pick is promoted on a fresh geocode by that same rule: the live geocoder is fuzzy, so its rows can carry places the query did not name, a "Vitória" inside a "Victoria" search, and such a row is searchable (`SearchCitiesAsync` keeps every row, deliberately) but unpickable and un-promotable; a persisted fuzzy pick left over from before the rule silently re-ranks to the city the user typed instead of resurrecting the wrong-city weather on restart), and the coordinate + label rules (`TryParseCoordinatePair` / `IsValidCoordinate` / `FormatCoordinates`, `ComposeLabel` / `ComposeZipLabel`, moved from the geocoder so the transport owns only HTTP + parse). The pure decision layer behind `WeatherGeocoder` (a leaf utility `WeatherClient` also calls directly for the forecast URL and the postal route decision). Every city-resolution fix commit's rule lives here with its comment. The cluster also uses `Core.Rendering.FontHelper` for the shared font cache, like every other widget. |
| **WeatherClient** | The weather fetch/cache/resolution orchestrator (Widgets): the `WeatherFetchResult` outcome union (each `Fetched`/`Tie`/`Stale` carries the `QueryKey` it was resolved for, the ADR-0006 carried key, pinned at the client's boundary; `Fetched` is fresh at the completion of the WHOLE capture window, through the cache write: the client re-validates after its save await, so an invalidation landing in the save window converts the verdict to `Stale`, the capture window itself being the named `CaptureWindowGuard`, whose `Dropped` predicate the three re-validation sites share), and the explicit-coordinates/postal/city/pick resolution legs delegating decisions to `WeatherGeocoder` (the postal leg routes per the resolver's `TryPostalRoute`, so ZIP input is not US-only), plus the snapshot assembly. The no-coordinates verdict splits a tie from a failure (ADR-0009): a `WeatherResolutionOutcome.Ambiguous` with candidates reports `Tie(candidates, queryKey`). The tied candidates (the widget's Location Match dropdown) ride the outcome instead of collapsing into a bare `Failed` dead end, so the dropdown's documented escape route is populated on the first fetch of a tied city; an `Unresolved` search and an empty-candidate tie stay `Failed`. Both still stamp the 5-minute cooldown, so a tied city never re-geocodes at frame rate. The fetch-control state machine (5-minute throttle window, single-flight claim, resolved-identity stamp, the mutable twin of the widget's gated `WeatherDisplayState`; both twins hold the shared `WeatherResolutionState` and route their drops through `WeatherInvalidation.Drop` via their single gated `Invalidate(kind)` entries) lives in `WeatherFetchControl`; the client exposes the single-flight claim as `IsFetchInFlight` (the one in-flight fact the display's staleness line reads, so the "Updating…" indicator tracks every fetch the client owns, the boot fetch and the flow's nested re-fetches included, instead of a widget-local flag); the cache file mechanics in `WeatherCacheStore`; the response parsing in `WeatherForecastParser`. The display strings never live here (that is the cluster's presentation modules'), and `LoadCacheAsync` documents its state-commitment: a successful load commits the cache's resolved identity + throttle into the fetch-control state, so a caller that DISCARDS the result must roll it back through `Invalidate(kind)` (the `Coordinates` kind). The interface says what the load did, so the rejection is the caller's job (the fetch flow enforces the rollback); the flow's own post-await re-validation guards only the return→apply gap the client's window cannot see. The throttle-stamped fact is exposed as the named `HasFetched` predicate (the flow's cadence gate reads it. Callers never compare the raw timestamp against `DateTime.MinValue`). |
| **WeatherCacheStore** | The weather cache file module (Widgets): the 1 MB size bound, the lazy name provider, the bounded stat-then-read load with the mid-read-growth guard, the identity stamp (`LocationQueryKey`) each snapshot is saved under, the atomic temp-file save, and `Clear()`. `WeatherClient` owns the staleness decision and applies the returned `WeatherCachePayload` only when the stamp matches the current query key. |
| **WeatherForecastParser** | The Open-Meteo response parser (Widgets): the modern-vs-legacy shape handling (`current` vs `current_weather`, `weather_code` vs `weathercode`), the humidity's current-block provenance (the hourly array's first bucket is stale-by-hours), the invariant day-name formatting with the "Today" marker, and the fetch-limit capping. `WeatherClient` keeps snapshot assembly and calls these pure rules per block. |
| **WeatherForecastLimits** | The fetch-limit constants (Widgets): `MaxFetchDays` / `MaxFetchHours`, the one place the forecast row caps are declared: the parser's strips and the cache store's deserialization both cap through it. |
| **WeatherFetchFlow** | The weather fetch-flow module (Widgets, ADR-0007): the SEQUENCE around one fetch: the location snapshot captured ONCE before the await (the key and the fetch resolve from the same record, so no seam read can drift between them, the former outcome-key vs. start-key comparison was tautological under one snapshot and is deleted; the client's carried key stays pinned at the client's boundary), the post-await live re-check against the live location (the client's `Stale` verdict covers the client's own window; the live re-check is the drop gate for an identity change the client's state does not see, a silent reassignment, the post-`InitializeAsync` profile hydration, verified for `Fetched` and the tie's `Tie` outcomes, ADR-0009), the drop-and-refetch routing (a dropped result force-refetches the new identity immediately, the in-flight claim that swallowed the edit-time force refresh is released by the completion), the resolved-label write-back gating (the `WeatherDisplayState.WritebackEligible` policy, the non-empty name, the CustomLabel veto, the no-op write, applied at the queue and re-evaluated under the display-state gate at the take, so a CustomLabel or Location edit landing after the queue is seen at the take, never sailed through an ungated flush check; the identity re-check at the set is one critical section), the tie apply (a `Tie` outcome rides the host's `TryApplyTie` seam: the gated candidates apply, the placeholder reset of the data state, and the `AppliedTie` verdict, ADR-0009), the single cadence gate (`CanFetch`: the static-snapshot veto once a fetch stamp exists, else the client's throttle window; force always eligible), and the boot-load rollback (the boot load takes the same one-snapshot rule; a discarded cache load rolls the client's committed identity state back through `Invalidate(kind)`; a version-only skip leaves nothing to undo). The widget keeps only host concerns, the property coercion (`BuildLocation`), the gate around the display state (owned by the `WeatherDisplayState` module, the widget's seam bodies are forwards), the UI-thread write-back flush, the context requests, and passes them across the typed `IWeatherFetchHost` seam (ADR-0008: the `CurrentLocation` coercion, the gated `TryApply(WeatherApplyRequest)`, the gated `TryApplyTie(candidates, identityGuard)` (ADR-0009), the gated write-back queue, the version / static-snapshot / run-token reads, and the two context requests). The widget IS the production host adapter (explicit implementations; the gate discipline spelled once in the display-state module) and the flow's tests wrap the SAME display-state module the widget uses, so `FetchLiveWeatherAsync` / `LoadCachedWeatherAsync` are forwards and the capture-window order (key captured before the await into the named `CaptureWindowGuard`, re-validated after, twice, the apply/write-back/tie seams re-deciding through its `StillCurrent`) is enforced in code here, not by comments at a call site. The verdict is assertable at the module interface without a widget instance, a render tick, or a host gate (`WeatherFetchFlowTests`). |
| **WeatherDisplayState** | The widget's gated display state (Widgets): the single gate, the snapshot state, the resolved-identity value (folded in from the deleted `WeatherResolvedIdentity` module, the shared `WeatherResolutionState` and the pending write-back live here directly), the last-success stamp, and the version-gated forecast render copies, every read and mutation under the SAME lock, so the "one consistent view" is a type, not a discipline repeated at every call site. The widget's `IWeatherFetchHost` seam bodies and the render tick's lock region are forwards over the module (`TryApply` / `TryApplyTie` / `QueueLabelWriteback` / `TakePendingWriteback` / `Invalidate(kind)` / `CaptureRenderView`), and the flow's test host wraps the SAME module, the flow's guarantees are pinned against the production gate shape, not a mirror. The write-back TAKE runs under the gate (a queue landing between the take's read and clear is unrepresentable) and re-decides eligibility there, the `WritebackEligible` policy (a non-empty name, the CustomLabel veto, the no-op-write veto) is the one spelling, evaluated at the queue AND the take, so the old gap (a CustomLabel set between the queue and an ungated flush check sailing through the flush) is unrepresentable: a veto refuses AND keeps the value queued (a "not yet", never a "never", the next frame re-decides against the current host facts); the Identity read hands out the immutable `WeatherResolutionState` record (its transitions run only through the gated members), and the last-success stamp resolves from an injected live-clock seam at stamp time (a test clock swap is observed). The module is parameterized by an injected neutral-location label (stored as a field, not a hardcoded client const) that `TryApplyTie` honors for a blank query, the seam is truthful, pinned by the test host injecting a distinct label. The tie reset bumps the data version AND the forecast version monotonically (the reset lands on the state's no-data fact, `HasData` false: the placeholder scalars are display seeds, so the tied state draws the no-data view — the em dash glyph — never a previous city's readings, ADR-0009's presentation follow-up resolved 2026-08-30); a re-apply landing on a previously rendered forecast version would make the capture's copy-skip reuse the previous city's forecast lists under the new city's header (the no-capture collision is pinned). `CaptureRenderView` derives the key's `LocationSet` from the location's emptiness (a location edit with no data-version bump must still rebuild the render model and flip the subtitle guidance), and its wide positional signature is deliberate, one production call site, the fields are plain inputs, and a snapshot record mirroring the key's property half would duplicate the key in miniature. Pinned at the module interface without a widget instance, a fetch, or an HTTP stub (`WeatherDisplayStateTests`). |
| **WeatherFetchFlowOutcome** | The fetch flow's per-fetch verdict union (Widgets): `Applied` / `AppliedTie` / `DroppedStale` / `Skipped` / `Cancelled`: what `WeatherFetchFlow.RunFetchAsync` (the widget's `FetchLiveWeatherAsync` forwards to it) actually did with a fetch's outcome: `AppliedTie` (ADR-0009) is the tie's named apply (the tied candidates populated the dropdown, the data state reset to its placeholder, the queried name became the header, no snapshot), so the flow is assertable without polling side effects. |
| **WeatherGeocoder** | The geocoding transport layer (Widgets): Open-Meteo/zippopotam HTTP legs with bounded reads and per-leg deadlines (the transport is a LIVE provider over the client's one `TestHttpClient` seam, resolved at request time, so a test's transport swap is observed on the next request and there is no sync step to drift), tolerant candidate/ZIP parsing, and the coordinate/postal/pick/city entry points (the resolution ladder: explicit coords → `"lat,lon"` pair → postal → warm pick → city): the postal leg runs zippopotam FIRST (60+ route countries, not US-only; zippopotam 404s on unsupported routes/full GB-CA codes) and falls back to the geocoder's own postal search HINTED-THEN-BARE (a leg that answers stops the sequence, and a hint the zippopotam index lacks must not leave the user with no weather); the pick-path fast path resolves a cached dropdown pick to its coordinates with ZERO HTTP (a pick absent from the cache falls through to the city leg, which re-geocodes and re-checks the pick through the resolver's exact-name + suffix/hint consistency rules); the city leg shapes the pick list (exact-name candidates only, postal queries keep every candidate, since the code matched their postcode index, not their names). Delegates every ranking/decision rule to `WeatherLocationResolver` (a leaf utility the client also calls directly for the forecast URL and the postal route decision). |
| **WeatherFetchResult** | The fetch-outcome union (Widgets): `Fetched`/`Tie`/`Throttled`/`InFlight`/`Failed`/`Stale`: a fetch result without a snapshot is unrepresentable, and a tie without candidates is unrepresentable (ADR-0009: `Tie` carries the tied candidates + the query key the geocoder refused to break, the widget's Location Match dropdown rides it, so a tied city is a pick to be made, not a dead end; the snapshot exists only on `Fetched`); the fetch flow distinguishes "try again now" (`Stale`) from "keep what you have" (the rest). |
| **WeatherInvalidation** | The resolved-identity invalidation rule (Widgets): the ONE property → drop-granularity map (`KindForProperty`: the `LocationMatch` pick → `Coordinates`, every other resolution input → `Location`, display-only properties → `None`) that the widget's `OnPropertyChanged` routes every resolution-input edit through, so the client's `WeatherFetchControl` twin and the widget's `WeatherDisplayState` twin can never drift on what an edit drops, `Coordinates` voids the resolved name/population (the widget twin's pending label write-back included, the client twin's identity query + throttle reset) but KEEPS the geocode candidates on both twins (a pick resolves against the candidates it was offered from); `Location` voids the WHOLE identity (candidates included) on both twins. The identity value itself is the shared `WeatherResolutionState` record (candidates, resolved name, population) both twins hold, and the single drop rule `Drop(kind, state)` applies the declared granularity to that value (None is the identity function; Coordinates drops name + population and KEEPS the candidates; Location empties it to the one `Empty` state). The twins' transitions route their shared part through it, then clear their OWN unique fields (the client's coordinates + query + throttle, the widget's pending write-back). The twin-equivalence pin drives both twins through every kind and compares their resulting identity values structurally (plus the pure `Drop` pins per kind), so a new resolved field added to the record is caught the moment a twin's transition forgets to route it through the rule (`WeatherInvalidationTests`). |
| **WeatherResolutionState** | The shared resolved-identity value (Widgets): the geocode candidates, the resolved header city name, and the resolved population, as ONE record, the value both invalidation twins hold (the client's `WeatherFetchControl` state machine and the widget's `WeatherDisplayState`, the module the deleted `WeatherResolvedIdentity` folded into). Declared once; dropped by the single rule `WeatherInvalidation.Drop`; the twins add their unique fields around it (coordinates + identity query + throttle on the client, the pending label write-back on the widget) and route their drops through the rule, so the shared part of what an edit drops can never drift between them. The null-keeps `With` is the "response omitted this section, keep the previous value" replacement rule; the one static `Empty` is the drop destination both twins land on. |
| **WidgetRouting** | The page-input routing module (Core): hit-test + touch routing over a page's placed widgets (rotated-local coordinates, highest-ZIndex selection). Extracted from the compositor so the renderer owns only rendering + edit state. |
| **TwitchChatStatusPolicy** | The Twitch chat's connection/buffer policy (Widgets): the IRC NOTICE→connection-state transition and the message-buffer cap, split from `TwitchChatPresentation` (display strings only); the `TwitchChatConnection` module is the one site that applies the transition and the cap. |
| **WidgetCreateResult** | The widget-creation outcome (Core): `Ok(widget)` / `NotFound` / `Broken(reason)`: a broken plugin is distinguishable from an absent one (the old null-or-null ambiguity), and `PluginInfo` is an immutable record. |

### Service Architecture

> The Windows Service was removed (ADR-0005). Terms below are kept only for
> historical context; the repo no longer contains a Service project, a WCF
> contract/client, or any service-routing machinery.

| Term | Definition |
|------|-----------|
| **Service** | ~~A Windows Service (`ModernWigiDashService`) running as LocalSystem that owns the USB device, captures telemetry, and exposes a WCF endpoint.~~ **Removed (ADR-0005)**: its frame-time role lives in PresentMon Service, its sensor role in LibreHardwareService, and the app talks USB directly. |
| **Service.Contracts** | ~~Shared assembly with the WCF contract and client.~~ **Removed (ADR-0005)**: its live telemetry DTOs (`SensorSnapshotDto`, `SensorReadingDto`, `FrameTimeSnapshotDto`) moved into `ModernWigiDash.Sdk` as plain data models. |
| **DetectServicePort** | ~~Protocol-verified service discovery.~~ Removed with the service (ADR-0005). |
| **ServiceRoutingState** | ~~Owns the App↔service routing truth.~~ Removed with the service (ADR-0005); poll loops are all direct producers now. |
| **ProfileOps** | The pure profile-operations module (Core): page CRUD, widget placement/rehydration (`PlaceWidget`, `PlaceCentered`, the centered variant reads the catalog entry's nominal default size, the widget's [WidgetMetadata] fact, so it never constructs a probe instance; rehydration repairs a size omitted from the imported JSON to the widget's declared preset, explicit sizes win, so a hand-crafted profile missing width/height rehydrates at the widget's own size, not the model's 2×2 default), the bounds-safe `TryGetPage` accessor (the window's delete confirm reads the page's confirm facts through it, so a stale index is a silent no-op in the window, never a throw ahead of `DeletePage`'s own validation), and JSON export/import (the one file boundary `ImportProfileFile`: exists → the size guard BEFORE any read → read → parse through `ImportJson`; the untrusted-input sanitization delegates to `ProfileImportSanitizer` and the named `ProfileImportOutcome` verdicts are the boundary's return, so the size rule is enforced at exactly one site the window and the boot load both route through). The boundary's one read also extracts the export bundle's OPTIONAL theme section (ADR-0021, `ProfileExportTheme.ReadTheme`, never throwing: a malformed section still loads the profile with a null theme), and the `Loaded` verdict carries it as `BundledTheme`, so no caller ever re-reads the file unguarded; `ExportJson` stays a BARE profile (the persisted profile.json never carries the section, pinned by test), so a hand-edited profile cannot smuggle in a theme, and the boot load ignores the verdict's theme field. MainWindow keeps only dialogs, selection, and refresh. The user-visible model mutations are testable through the module's interface. |
| **ProfileImportSanitizer** | The untrusted-import sanitization (Core/Models): the caps and value rules every foreign profile passes before rehydration, the page/widget-count DoS caps, the null/empty collection repair (pages, page elements, `-widgets`, `propertyValues`), the page-background and widget image/icon paths restricted to safe relative paths (rooted/UNC/drive-relative/`..` rejected), the unconditional ActionCommand clear (Process.Start/SendInput), the IRC-channel sanitization via `TwitchChannelRule`, the InstanceId regenerate-or-dedup rule (its `IsSafeInstanceId` is also the weather widget's defense-in-depth cache-file-key guard), and the string-value bound. The import file-size guard (`MaxImportFileBytes` + `IsImportFileTooLarge`) lives with the rules it protects and is enforced at exactly one site: `ProfileOps.ImportProfileFile`, the one file-import boundary (exists → guard before any read → read → `ImportJson`) the window and the boot load both route through; the boundary's named `ProfileImportOutcome` verdicts (`Loaded`/`Absent`/`TooLarge`/`Failed`) are what the window maps to its dialogs and the boot load to its log, and the app's own persisted profile passes the boundary as trusted input. |
| **ProfileMutationShape** | The window's one post-mutation contract (App): `MainWindow.ApplyProfileMutation(shape, selection)` is the single entry every profile mutation routes through after it lands. The `shape` selects the refresh bundle: `Transform` (in-page state only: place/remove/clear, transform/opacity write-backs, snap-to-grid, page background) re-syncs nothing structural; `Structural` (add/delete/rename/switch page) re-syncs the tab strip; `RawWrite` (an import) re-syncs the tab strip plus the snap-to-grid toggle from the imported page (absorbing the import's old direct control write). Every shape then re-applies the selection, refreshes the active count, repaints the canvas, and marks the profile dirty exactly once, so no call site re-derives "what happens after a mutation" and the old doubled/missing marks are unrepresentable. Inspector-driven transform/opacity write-backs are the one path that marks through the controller's `onProfileChanged` callback instead (one invocation per landed write-back; the window's forwarding handlers add none); property-value commits mark through the commit owner's `PersistProperty`. `SelectWidget`'s same-reference early-out keeps the idempotent re-entry on the import path from re-applying a disposed selected instance. |
| **StartupWiring** | The window's startup wiring (App): the ordered construction steps as named items (`FrameDelivery`, `Telemetry`, `WidgetCatalog`, `ProfilePersistence`, `HostModules` - input controller + drain + dialog host + inspector + page tabs + background picker, `ProfileLoad`, `SnapToGridResync`, `DeviceTouchRoute`, `FramePump`, `PowerLifecycle`, `TeardownHook`, `InitialRefresh`, `EditModeResync`, `Tray`, `CloseIntercept`, and the `Wired` arm last) - one named artifact the constructor applies in order, the `TeardownPlan` image for startup. The sequence is the load-bearing knowledge (the host modules before the profile load: a widget's `InitializeAsync` runs synchronously inside the load and calls back into the context; the state resyncs before the wired arm, so their XAML events stay guarded; the wired arm last, so the guarded handlers arm only after every module exists) and is pinned against the real list by `StartupWiringTests`, the way `TeardownPlanTests` pins the teardown sequence. The wiring-assigned fields are `null!`-typed (the pre-module window is type-visible) and the context's module-deref callbacks are null-tolerant for that window, so a reorder degrades to a benign no-op; the artifact's final `InitialRefresh` re-establishes the inspector panel, so a lost pre-module refresh costs nothing. |
| **PollLoop** | One parameterized poll loop (Sdk): owns its cancellation lifecycle, readiness guard, failure logging, and inter-tick delay. The App's two direct producers (sensor 1s, frame-time 1s) and the engine's direct-USB touch loop (16ms) are all instances with injected probes and sample sinks, one loop shape, every hop. |
| **Standby** | The display's off state: the built-in vendor Welcome screen with the backlight turned off by the vendor's immediate-sleep command (0x13, verified against the vendor Manager's decompilation: its `SnoozeDevice` is the "Put the Device To Sleep" action, and its exit ritual is the Welcome screen followed by the sleep command). Entered on the app-close path. The engine's `Dispose` sends `GoToStandby`, which runs that vendor ritual; the display has no active auto-sleep of its own, so without the sleep command it would idle on the Welcome screen with the backlight on (the old "sleeps on its own timeout" assumption was wrong, observed on-device 2026-08-21). The display stays USB-powered while asleep and accepts control transfers (the touch report's byte 7 carries the sleep state), so app start wakes it explicitly: the init sequence opens with the vendor's wake command (0x12, the Manager's `WakeDevice` = `ClearScreenTimeout`) before the brightness/page/frame work, and a physical touch wakes an asleep display too (verified on-device 2026-08-21: tap on the dark panel relit the Welcome screen with the app closed). The standby verdict is always observable in the display log: a not-confirmed standby (no live connection, or the writes failed) logs a failure line, and the App's `Exit` handler writes an exit marker and runs `FileLog`'s one final `Flush`, the cadence flushes (8 KB / 250 ms) alone left the standby line, the last line before exit, in the buffer on one on-device close (2026-08-21). The standby handshake is the device's only clean release: a force-killed app (no close path) can wedge the display's bulk pipe. Later connects pass PING/control writes but hang on the init blank-frame write, leaving the engine stuck in `Connecting` with no frames, and recovery is a physical USB unplug/replug (observed on-device 2026-08-21). |

### Telemetry & Data Flow

| Term | Definition |
|------|-----------|
| **LhmSensorStore** | Static in-process cache of hardware sensor readings from LibreHardwareService. A facade over `TelemetryStoreFacade<SensorSnapshotDto>` (Sdk), the snapshot shape is the DTO itself (no shadow record); the reading label (`HardwareName: SensorName`) derives as a computed property on the DTO, the single derivation site the picker and widgets share. Written by the App's shared-memory producer (`LhmSharedMemoryReader` → `UpdateFromDto`, producer timestamp preserved); read by `HardwareMonitorWidget` through `TryReadFresh`, the store owns the staleness decision, consumers cannot skip it. |
| **ILhmMapSource** | The named-map I/O seam behind the LHS reader (Hardware/App): one mutex-guarded, bounded copy of the sensors map, or null with an error. The real adapter (`MemoryMappedLhmMapSource`) owns the MemoryMappedFile/Mutex specifics and the attacker-claimed-size copy caps; in-memory fakes drive the reader's Poll policy in tests. |
| **FrameTimeStore** | Static in-process cache of FPS/frame-time snapshots. A facade over `TelemetryStoreFacade<FrameTimeSnapshotDto>` (Sdk) with the same shape as `LhmSensorStore`, the snapshot shape is the DTO itself (no shadow record). Written by the PresentMon producer (see below) via `FrameTimeStore.UpdateFromDto` (producer timestamp preserved); read by `FrameTimeWidget` through `TryReadFresh`. The widget renders only PresentMon-reported data: an 8-metric dashboard view (FPS/frame time hero, 1% low, 0.1% low, CPU frame, GPU busy %, displayed FPS, dropped frames, GPU time, present mode) that tap-toggles to a PresentMon-overlay-style readout; both views shrink gracefully and show zero values when no process is tracked (PresentMon's own 0-presents reading; no monitor-refresh-rate display, that is not a PresentMon feature) **and when the tracked target is not actually displayed**: the producer's target-trust policy (`TargetTrustPolicy`: settling window, adoption, frozen-data guard) reports live data only for the pid that produced the last live sample: on a foreground switch it holds the zero state through a settling window (PresentMon returns the departed target's frozen data for every pid after a switch, observed on-device), an adopted target's data is only reported once it differs from the departed target's last values, and a presenting-but-not-displayed target (backgrounded fullscreen game, `PM_METRIC_DISPLAYED_FPS` = 0) also reads as the idle zero state. |
| **TelemetryStoreFacade** | The store-facade shape (Sdk): owns one `TelemetryStore<TRecord>` instance bound to the domain's empty value and staleness window, and adds the null-tolerant producer write plus the fake-clock test seams. `LhmSensorStore` and `FrameTimeStore` wrap one instance each, the staleness policy and its test surface are declared exactly once (the former `StaticTelemetryStore` pass-through layer was folded into the facade). |
| **PresentMon producer** | The frame-time source (ADR-0003). Replaces the deleted `FrameTimeReader`: the app connects to PresentMon Service (`pmOpenSession`, non-elevated), resolves a target PID (preferred foreground window, else most-active presenter), calls `pmStartTrackingProcess`, and polls a rolling 1s dynamic query (`PM_METRIC_PRESENTED_FPS` AVG/P99/P01, `CPU_FRAME_TIME`, `GPU_TIME`, `GPU_BUSY`, `DISPLAYED_FPS`, `DROPPED_FRAMES`, `PRESENT_MODE`, `APPLICATION`) on the existing 1s `PollLoop` shape. Results map into `FrameTimeSnapshotDto` → `FrameTimeStore.Update`. Loads `PresentMonAPI2.dll` from the service SDK dir at runtime, never ships its own copy (client↔service binary protocol isn't backward-guaranteed, issue #383). Absent service ⇒ widget shows a graceful "PresentMon not installed" state. |
| **PresentMonMetricCatalog** | The installed service's metric truth (App/PresentMon): built once per session from `pmGetIntrospectionRoot`, it exposes each metric's type/unit and the stats the service accepts. `PresentMonQueryBuilder` builds the dynamic query from wanted specs against the catalog, an unsupported metric degrades to a named drop (the field reads no-data) instead of an opaque QueryMalformed failure, and a rejected preferred stat falls back to the first allowed one (PRESENT_MODE only accepts NEWEST_POINT/MID_LERP). |
| **PresentMonQueryRegistry** | The query subsystem (App/PresentMon): registers the dynamic + frame queries, owns the elements, the field→element map, the blob strides, and the poll/drain loops (capacity growth, bounded drain). Pure policy over injected delegates, so the loops are unit-testable without the DLL; `PresentMonNative` keeps only the session and tracking surface. Enum-typed metrics (PRESENT_MODE) are read as 4-byte int32 per the element's DataSize, not as doubles. |
| **FrameTimeSnapshotFactory** | The only place the frame-time snapshot gets shaped (App/PresentMon): unavailable/idle/capture-dead outcomes and the live mapping with its unit conversions (1000/FPS frame time, GPU busy ms → busy-per-frame %). Extracted from the producer's poll method so the conversions are directly testable. |
| **FrameTimePresentation** | The widget's display rules (Widgets): given a snapshot and placement size, the hero strings, the eight dashboard cards, the nine overlay rows (PresentMon's metric names; 1000/percentile-fps frame times), and the shrink visibility flags. The widget's render methods are thin adapters over it. The display is assertable without pixels. |
| **TrackedTargetResolver** | The PresentMon tracking-target selection (App/PresentMon): which process to track: the preferred foreground window, else the most-active presenter, with injected foreground/children probes (real user32/toolhelp adapters, test fakes) so resolution is drivable without real processes. |
| **LibreHardwareService producer** | The hardware-sensor source (ADR-0004). Replaces the deleted `LhmSensorReader`: LibreHardwareService (LocalSystem) owns the hardware polling and publishes readings to named shared-memory maps (`sensors`, `status`; `all-hardware` optional) guarded by named mutexes. The App's `LhmSharedMemoryReader` reads via the `ILhmMapSource` seam (one mutex-guarded, bounded copy), parses the header (`MetaDataSize`, `UpdateInterval`, `LastUpdate`, `index-length/offset`, `index-format`, `data-length/offset`) and honors the declared index format (JSON or MessagePack, MessagePack-CSharp package). DataSensor records map 1:1 into `SensorSnapshotDto` (SensorId preserved; LHS publishes value/min/max only, no Avg; `UnitFor(SensorType)` replicated app-side) → `LhmSensorStore.UpdateFromDto` on the existing 1s `PollLoop` shape. Absent service ⇒ widget shows a graceful "LibreHardwareService not running" state. |
| **UpdateFromDto** | Maps sensor DTOs to widget-side records on the store. Centralizes the DTO→render-model translation in the store layer. |

### Widgets

| Term | Definition |
|------|-----------|
| **Widget metadata** | `[WidgetMetadataAttribute]` defines the plugin ID, display name, category, and the nominal default grid size (`DefaultGridSize`, a `GridSizePreset`; the 2×2 house default), the single declaration site: the catalog resolves it at registration (the `PluginInfo.DefaultSize` placement math reads), and `ModernWidgetBase.DefaultSize` derives the instance's size from the same attribute, so a widget that only declares its preset needs no override and `PlaceCentered` is pure math over the catalog (the probe-instance construct-and-dispose trap is gone; its inert-constructor dependency with it). A non-preset default can still override `DefaultSize`. Read by `WidgetPluginLoader` for catalog registration. |
| **Widget property** | `[WidgetPropertyAttribute]` on a widget's public property defines the inspector UI (text/number/boolean/color/choice). Widgets implement `IWidgetPropertyOptionsProvider` for dynamic choice lists. |
| **Widget action** | `IWidgetActionInvoker` / `IWidgetActionPresentationProvider`: widgets that expose executable actions (e.g., Twitch login, hotkey execution). |
| **LocationSearchModel** | The inspector's Location-search editor rules (App): the seed text (label + compact population suffix), the tick's query and the pick-less commit (the seeded suffix is display-only, it searches and commits as the base label so a seeded box never degrades the next resolution to a bare-name tie; a real edit takes over verbatim), the focus-loss veto while a pick is in progress in the results popup, the debounce tick's version-token stale rule, and the shared compact population format. `InspectorPanelRenderer` keeps only the WPF wiring of the box/popup/results list over this model. |
| **IconValuePolicy** | The inspector's icon-value rules (App): whether a value names a bundled Griddy icon or a custom SVG file (`IsNamed`/`IsCustom`, the catalog's `Contains` as the one predicate), the read precedence (the icon file wins over the named icon when both companion properties hold a value, `ResolveCurrent`), and the write-back mutual exclusion (`SplitWriteback`: exactly one of the companion properties holds the chosen value). The icon picker's decision model (`IconPickerModel`) and the inspector's write-back routing decide through it, so the two WPF sites and the tests cross one seam and the rules are pinned without pixels (`IconValuePolicyTests`). |
| **IconPickerModel** | The icon picker's decision model (App): the search filter over the Griddy catalog (blank filter shows every name, else the case-insensitive contains rule), the named-or-custom selection, the custom chip text (the one `Custom: {path}` spelling; the chip follows the selection, so a named pick never leaves a stale custom label behind), the case-insensitive highlight, and the accept verdict (a blank choice is null, so the Select button can never accept an empty value). `DialogHost.ShowIconPicker` is a thin adapter over it: the chrome builds the cells from `VisibleNames` and routes every click through the model, so the picker rules are pinned without a window (`IconPickerModelTests`). |
| **DeviceAuthorizationModel** | The device-authorization window's decision model (App): the display facts (title, header, code, verification text, expiration text), the trusted-open verdict (a tampered verification URL, anything but https on twitch.tv, is refused and logged by the shared `TrustedUriPolicy` gate before the shell-open seam runs; the open seam's failure line is spelled here too), the copy-code verdict (the clipboard seam + its failure line), and the lifetime's slot-clear identity rule (only the current window's close clears the host's slot, so a replaced window's late close never clears the replacement). `DialogHost.ShowDeviceAuthorization` is a thin adapter over it, so the open/copy/lifetime rules are pinned without a window (`DeviceAuthorizationModelTests`). |
| **Inspector panel** | The right-side settings panel in MainWindow; populates UI from `[WidgetProperty]` attributes and calls `ApplyInspectorPropertyValue` to write values back. The reflection→editor mapping is a pure `EditorDescription` model (no WPF, no dialogs); a thin WPF mapper renders it, and every editor write-back funnels through the controller's single `ApplyPropertyValue` seam, the re-entrancy guard lives at that funnel (an unguarded write-back path is unrepresentable, so the boolean editor needs no special casing), and the transform write-back parses only the fired box (an unparseable sibling box fires no mark and no repaint). `InspectorController` takes two narrow facet adapters, `TransformFieldBindings` (the six transform boxes + opacity slider + canvas-render request) and `CustomPropertyPanel` (empty/active shells, name label, custom-properties host + focus capture/restore), instead of a 13-element host bag, so adding a field touches one record, not the window's wiring. Dynamic choice lists resolve through `IWidgetPropertyOptionsProvider`, no widget-specific `typeof` checks. |
| **TextRenderHelper** | Shared rendering utilities: text truncation with ellipsis, centered text drawing, title/subtitle placeholders, only the multi-consumer text rules remain here (≥2 production consumers each). The sparkline surface moved to its own module (`SparklineRenderer`), greedy word-wrap is private to `WrapCache` (the wrap result's only consumer), and the SVG-path helpers live in `SvgIconHelper`. |
| **SparklineRenderer** | The sparkline module (Widgets): the span path builder is the single geometry implementation; the entry points are `DrawSparkline` (list, draws with its own paints), `BuildSparklinePaths` (list, hands the caller paths to cache), and `RebuildSparklinePaths` (span, rewinds caller-owned paths in place). Owned by the two telemetry widgets, `HardwareMonitorWidget`'s graph mode rebuilds its caller-owned paths in place per frame (no SKPath allocation), `FrameTimeWidget` caches the built paths per samples identity. |
| **MediaSessionMonitor** | The media-session module (Widgets): owns the SMTC subsystem (manager bootstrap, current-session tracking, per-session media-properties / playback / timeline events, the version-token-guarded snapshot refresh, the artwork-key derivation with its track-transition reload token) behind the internal `IMediaSessionSource` seam, and the playback state it carries is the neutral vocabulary: `MediaPlaybackStatus` / `MediaRepeatMode` mirror the SMTC ordinals, so the one edge mapping in `WinRtMediaSessionSource` is a range check plus a cast (the only file that knows the WinRT SMTC surface; the thumbnail `IRandomAccessStreamReference` stays WinRT by design, the artwork pipeline's blob). The tap actions (`TogglePlayPause` / `ToggleShuffle` / `CycleRepeat` / `SeekToRatio`) decide can-run and argument from the monitor's own latest snapshot, so a tap can never send a desired state computed from a snapshot a caller held; the widget sends intents only, and the raw commands (`Play` / `Pause` / `Next` / `Previous` / `SetShuffle` / `SetRepeat` / `Seek`) are the routing surface the policy dispatches onto and the tests pin. |
| **NowPlayingPresentation** | The Now Playing widget's display rules (Widgets): meta line, time format, the ordered friendly-app-name map (token → display name, first matching rule wins; the "Apple.iTunes" → "iTunes" precedence is load-bearing and pinned by test), progress/seek ratios, playback-rate text, the idle verdict (`IsIdle`: no snapshot, or a Closed/Stopped session draws the idle panel; the `NotNullWhen(false)` predicate carries the "non-idle implies a live snapshot" fact into the widget's flow analysis), the seek-tap enablement rule (`CanSeekNow`: the session reports seekable AND carries a positive duration), and the repeat-mode cycle (`NextRepeatMode`: None → List → Track → None, an unknown mode degrades to the cycle's start) as the one spelling the monitor's tap policy applies to its live state. The only media state it reads is the seam's neutral vocabulary, so it is truly WinRT-free; the widget draws the model. |
| **HotkeyActionCatalog** | The hotkey action-type vocabulary (Widgets): the one owner of the name set the inspector presents, the name-to-(kind, fixed media-key) mapping, the default (`DefaultName`), and the `NeedsCommand` rule. `Create` and `NeedsCommand` read `Entries` here, and the widget's [WidgetProperty] choice array (a compile-time attribute literal, which cannot bind a runtime value) is pinned to the catalog in lockstep by test, so a renamed or hand-edited choice fails the pin instead of sailing to the Launch default. An unknown name (a hand-edited profile) degrades to the Launch kind, and an empty command then fails the executor's path validation (the pinned unknown-name rule), never a silent no-op. |
| **NowPlayingLayout** | The Now Playing widget's touch-zone layout (Widgets): the hit regions and their precedence (shuffle/previous/play-pause/next/repeat/seek) derived from the placement size. The widget's Render draws from the same record its OnTouch hit-tests against, display and input share one geometry source; the record also carries the scaled pad/art-gap (the 24/30 design constants live only in `Compute`, never re-derived at a draw site) and the design base aliases `DisplayGeometry`, so it cannot drift from the framebuffer area. |
| **AudioCaptureLifecycle** | The visualizer's capture-lifecycle module (Widgets): owns when a capture source is alive and how it dies. Capture is tied to rendering: it starts on the first `OnRender` (the widget's page becoming active) and stops when renders stop for a 1s grace (page switched away), so a hidden widget's WASAPI loopback capture never runs forever. The lock protocol (one lock serializing the render-thread start, the capture-thread watchdog stop, and the thread-pool deferred dispose: a watchdog firing as the page switches back can never unsubscribe/dispose a source mid-start), the stale-render watchdog (1s grace against the injected clock, the timestamp primed before start so the first callback cannot kill a fresh capture), and the deferred-stop marshaling (NAudio's `WasapiCapture.Dispose` joins the capture thread, so stopping inside the `DataAvailable` callback would self-join; the stop runs on the thread pool, at most one queued) are owned here. A failed start disposes the half-opened source, logs once, and retries on the next render tick (the old inline version marked the widget as capturing on the swallowed failure and wedged). The widget drives it from `Render` (`OnRender`) and `DisposeAsync` and draws one snapshot; it is the seam's only consumer. |
| **AudioFrameBuffer** | The thread-safe front of the visualizer's DSP (Widgets): owns the gate around the pure `AudioSpectrumAnalyzer` and the double-buffered output copies, the capture-lifecycle module feeds sample blocks on the capture thread, the render thread takes one `Snapshot` per frame and draws the copy; the widget never holds the gate while drawing, and no array is allocated per frame. |
| **FeedSubscription** | One ticker widget's feed-identity ownership (Widgets): diffs the tracked identity against the last one, releases the old claim, subscribes the new one, and seeds the fallback fetch, the subscription bookkeeping is testable without a widget instance. |
| **PictureSourcePolicy** | The picture widget's source-resolution policy (Widgets): how (source mode, path-is-file, path-is-folder) resolves to a single file or a cycling folder, when a tap promises picture cycling (`CanCycle`), and the placeholder hint that follows that verdict. The mode table lives in exactly one place, the render placeholder, the touch handler, and the active-file selection all consume the verdict (an unknown hand-edited mode behaves like Auto; a forced "Folder (Cycle)" whose folder does not exist no longer promises a tap-to-cycle it cannot keep). |
| **MediaDecoder** | The picture widget's media-decode module (Widgets): `Decode(byte[], logRefusal)` never throws and returns a `MediaDecodeResult` (an animated frame set + per-frame durations, a still bitmap, or `None`) with every partial bitmap disposed on failure (the frame arrays are allocated before the per-frame loop and filled through ref, so a mid-loop throw still exposes the decoded prefix to the caller's catch). The bomb caps (32 MB file, 256 frames, 4096 px/frame, 512 MB total) refuse hostile media through the caller-owned log seam, the delta-composite path (CopyTo before GetPixels, the heap-corruption order) is owned here, and single-frame media routes to the still path, so `Frames` is null or has 2+ elements (`IsAnimated` is unrepresentable-false for a non-null `Frames`). The widget keeps only the file read, the version-token check, and the publish: a `None` result published from a refused or failed decode retires the previously installed media and drops to the placeholder (a file that no longer decodes never leaves the previous file's pixels on screen). `MediaDecoderTests` pins decode, the refusals (file-byte, frame-count, per-frame pixel, and total-frame-bytes boundaries over binary GIF fixtures, the two size clauses over LSD-patched copies), and delta compositing. |
| **SvgIconHelper** | Shared parsed-SVG-path helpers (Widgets): the draw-scaling protocol and the parse cache used by the bundled Griddy icon set and runtime-loaded icon files. Split out of TextRenderHelper so the text/sparkline helpers stay free of icon machinery. |
| **WeatherPresentation** | The Weather widget's display rules (Widgets): WMO code table, the current-condition icon's day/night rule (`MapWmoIcon`: clear and mainly-clear read as a moon at night, partly-cloudy as the night city, every other code keeps its day icon, the WMO descriptions stay day-neutral and `MapWmoCode` is untouched), unit-system parsing, temperature/speed formatting, the per-mode pill/row strings, the no-data display (the em dash glyph in place of the hero temperature, no pills or forecast strings: the placeholder scalars are display seeds and never render as weather — the never-fetched and the tied states, ADR-0009's follow-up resolved 2026-08-30), the subtitle guidance line (the four strings + the priority order + the unresolved-header verdict it recognizes via the neutral `UnknownLocationLabel`, all display facts owned here, so the const no longer lives in fetch-control state and the widget injects it into the display state), the header staleness line (Updating… / time-ago / nothing), the time-ago format, and the header title's location-visibility rule (the widget's `HideLocation` toggle, ADR-0019: on, the title renders nothing, both the resolved city and the UNKNOWN LOCATION placeholder being location names, while the CustomLabel, a label not a name, and the guidance lines, guidance not names, keep rendering; the value rides `WeatherRenderModelKey` like the other visibility toggles) plus the deleted custom-label confirmation subtitle (a CustomLabel renders nothing underneath it, the "still shows underneath" case unrepresentable). The format helpers live here (not in the data module); `WeatherClient` stays fetch/cache/resolution. The display strings never live here. The night icon flips on the next fetch after the day/night transition (subject to the 5-minute throttle, the same staleness as every other current-condition value). |
| **WeatherRenderModel** | The cached render model (Widgets): every formatted string the five layout modes draw plus the data slices, keyed by the `WeatherRenderModelKey` record (data version + bounds + the property snapshot + the location's emptiness, `LocationSet`, + the data fact, `HasData`: the no-data view changes the pane without any string of its own, so the flag must be part of the identity) built once per build, recomputed only when a single record comparison fails, and the null `Key` on a never-built model makes a cache hit unrepresentable without identity. The key rides the model BY REFERENCE as its single identity, the property snapshot the draw paths read (e.g. `ShowForecast`) comes from the key itself; the old field-by-field copy was removed, so key/copy drift is unrepresentable. The build is owned by `WeatherRenderModelFactory` (`Resolve(cached, inputs)`): the key hit returns the cached instance (the per-frame path allocates nothing), and the miss composes the display facts through `WeatherPresentation`, the auto-truncated header at the max width the draw path uses (custom label first), and the pill widths through the renderer's shared measurement, the per-frame geometry (the `WeatherLayout` header + scale) is computed ONCE in the render tick and handed in as `WeatherRenderModelInputs`, so the build never re-derives the geometry it is handed; the widget's render keeps only the gate reads, the inputs assembly, and the cache slot (a new model replaces the old on reference change). |
| **WeatherWidgetRenderer** | The widget's draw paths (Widgets): the five layout-mode render methods over a `WeatherRenderModel` (each gates on the model's data fact: while no snapshot is committed the pane draws its no-data view — the one centered glyph — and never the hero or the strips), thin adapters over `WeatherPresentation`'s display facts and `WeatherLayout`'s geometry, the only consumer is `WeatherForecastWidget`. |
| **WeatherLayout** | The Weather widget's pure layout module (Widgets): the scale factors, the header geometry (title, unit badge, content pad) + header touch zones computed once per frame so the drawn badge and the tap targets share one source of truth, the hero/pill shrink rules, and the layout-mode catalog, `DefaultMode` + the `Modes` `ModeEntry` list (mode + persisted/display name) as the ONE `static readonly` spelling of cycle order and display names; `ParseMode` is the single string→mode mapping site and the `NextMode` cycle and the render's fallback read the catalog. The inspector property's `[WidgetProperty]` choice array is a compile-time literal copy of the catalog (attributes cannot bind a runtime value), a lockstep pin `LayoutModeAttribute_Options_MatchTheModeCatalog` keeps the two in agreement, so a renamed or hand-edited mode name fails a pin instead of silently breaking the cycle, and the 30 FPS render path allocates nothing per tick. |
| **TwitchChatPresentation** | The Twitch chat widget's display rules (Widgets): the `ChatState` status payload and its named transition factories, the ONE spelling of every status detail the widget's state-change sites store (idle, connecting, joining-#channel, reconnecting, LIVE, login-failed; the plain-disconnected state is the only empty-detail payload), plus the header status line over that payload and the empty-state hint per `ChatStatus`. Pure and assertable. |
| **TwitchChatConnection** | The Twitch chat's connection module (Widgets): the IRC endpoint, the anonymous `justinfan` credentials, the four-line CAP/PASS/NICK/JOIN handshake, the PONG keepalive with its token-retirement protocol (a live token is cancelled and dropped, never disposed while a fire-and-forget PONG task may still hold it; the terminal dispose sits in `DisposeAsync`, after the feed loop has unwound), the `FeedLoop` reconnect wiring (exponential backoff, one state per cycle end and per stop), the single owner of the `ChatState` transition (one volatile reference, the NOTICE rule through `TwitchChatStatusPolicy`, the login-failure line through the error log), and the clamped message buffer (append-trim under one lock; the render snapshot is replaced wholesale, so the render thread reads without a per-frame copy). The seam is the shared `IWebSocketFeed`: production binds `ClientWebSocketFeed`, tests bind the in-memory `FakeFeed`, so the whole connection is drivable without a network or a widget instance. The `onChanged` hook fires on every state transition and buffer append; the widget binds the repaint request, and the tests bind a transition recorder, because the fake's queue drains faster than a poll can sample, so the transient states (Live, LoginFailed) are asserted from the recorded transition, not the current value. `TwitchIrcMessages.NormalizeChannel` (trim, hash-drop, lowercase; an invalid name, an embedded CRLF, or an over-the-25-char-cap name falls back to the default channel) is the one gate between the widget's channel property and the JOIN line, and the header badge reads the same rule. The widget keeps the property surface, the rendering, and the touch toggle, and drives the module from live property reads (a test's post-ctor `FeedFactory` swap and an inspector write both take effect). |
| **PriceFeedMessages** | The price-feed wire format (Widgets), both sides: the WebSocket subscribe frames (the request side: the Binance one-frame-with-every-stream-key frame and the `usdt@ticker` stream key it carries, the Finnhub one-frame-per-symbol frame, one spelling each shared by the connect-time send and the incremental per-symbol send, so the two can never drift) and the payload parsers (the response side: Binance WS/REST, Finnhub WS/REST, CoinGecko simple-price). Pure and directly tested; PriceFeedManager's handlers, poll bodies, and subscribe sends are thin write adapters over them. |
| **UsbBadgeModel / CatalogFilter** | The window's last stateful display logic (App), extracted: the USB engine state → (label, brush) mapping and the widget-catalog filter/sort, pure and assertable without WPF. |
| **PageTabsView / PageTabVisual** | The page-tabs strip module (App): owns tab construction, the wheel scroll, and scroll-into-view; the window keeps only the switch/rename/delete page seams. The pure per-tab geometry rules (`PageTabVisual`: padding/margins derived from active and delete state) have no UI tree, the constants are pinned by tests. |
| **ClockPresentation / StopwatchPresentation / TickerPresentation** | The clock, stopwatch, and ticker widgets' display rules (Widgets): the digital time strings with the `ShowSeconds` seconds variants (the widget's `ShowSeconds` toggle, ADR-0019: `hh:mm`/`HH:mm` become `hh:mm:ss`/`HH:mm:ss`; AM/PM + the date badge and the analog mode untouched, the existing width-divisor font fit covering the 8-character string), the AM/PM + date strings, mm:ss.cc elapsed + status text, the price-decimal tier rule + label fallback, pure and assertable. |
| **ChunkedBulkWrite** | The LibUsb chunked bulk-write policy (Hardware): bounded chunks sized for the legacy libusb driver, advancing by the actually-transferred length so short writes never skip a gap. Pure over a write delegate, the only formerly zero-test module in the repo is now fully covered. |
| **PriceFeedManager** | Shared price streaming for stock/crypto/FX tickers (Widgets): ref-counted subscription claims, the two WebSocket feed loops, and the REST cycle wiring. The price map and its merge policy are the **PriceMapStore** seam: every store site routes through the store's live/fallback rules. The per-kind subscription wiring (validation guard, ref-counted map, first-claim startup, the kind's own loop/REST order and gates, the WS subscribe trigger (the key, the frame's spelling living in `PriceFeedMessages`), and the one-shot seed leg (null when the kind's cycle already serves it, FX)) is the three-entry `FeedKindWiring` table `Subscribe`, `Unsubscribe`, and `SeedFallbackAsync` all route through, so the sequences (validate → claim → start → subscribe; the seed's fetch → apply) and the kind→(map, seed) mapping are each owned once, the kind→wiring lookup carries a named arm per kind (an unnamed value fails loudly, never a silent default), and a fourth asset kind is one table row. The first-claim startup and the last-release teardown are two non-atomic field sequences over the same loop fields, so the manager's lifecycle gate makes them one serialized unit (a 2026-08-26 data race: the racing teardown's null could land between the `??=` create and the `Start`, an NRE the coverage instrumentation's widened window surfaced; the in-gate dispose re-check and the `EnsureActive` re-arm keep a disposed manager and a dead cts from starting a loop). One REST quote leg per source sits behind the manager's shared process-wide HttpClient (which it never owns), legs own URL shape and response parse, so the manager never builds a URL or parses a source payload itself. The one-shot fallback seed (`SeedFallbackAsync`) is the manager's single seeding operation riding the kind's seed leg from the table (crypto → CoinGecko leg, stock → Yahoo leg, FX none, the Frankfurter cycle already serves it) and applying the sample through the store's fallback rule, and it never throws, the ticker widget owns no part of the seed (its render tick pulls the seeded price from the shared map). |
| **PriceMapStore** | The price-map store seam (Widgets): the one owner of the shared price map and of every write into it, over one clock (the manager's `Clock` read live at write time, the Frankfurter-leg pattern, so a test that swaps the clock after construction is still honored). Two named merge rules: `ApplyLive` (a live source always overwrites the price and timestamp, at any age and from any source; a sample with no change figure, a Finnhub trade or a quote missing its day change, keeps the previously known change instead of zeroing it) and `ApplyFallback` (a fallback sample, the one-shot seed or the crypto cycle's batch tail, writes only past the `ShouldKeepExisting` downgrade guard, with the same change-keep), plus the read/release seams (`TryGet`/`TryRemove`) the manager's `GetPrice`/`Unsubscribe` route through. The legs are pure fetchers and the WS parse handlers route into the rules, so the former five hand-rolled write sites (three merge behaviours, one named guard, a hand-rolled variant at the batch tail) are a rule choice at a named entry point, and `ShouldKeepExisting` moved here with its pins. |
| **PriceRestLeg** | One REST quote leg (Widgets): the source adapter behind the manager's HTTP seam, validation guard → URL build for the symbol → one GET → the source's wire parse (the parsers live in `PriceFeedMessages`) → a `QuoteSample`. The leg owns the source's wire format, never the price map, the manager's `PriceMapStore` applies the sample with its merge rules, so a URL or parse bug localizes in one construction site per source. The Frankfurter leg's date window is built from the manager's live `Clock` at poll time. |
| **CoinGeckoRestLeg** | The CoinGecko simple-price leg (Widgets): one URL shape for both consumers, the crypto cycle's batch fallback (distinct CoinGecko ids among the subscribed base coins in a single `ids=` request, one parse) and the widget's one-shot fetch (one id resolved from the `SymbolCatalog` table, an unknown coin makes no request). The price-map merge policy (including the BinanceUS freshness guard) stays with the manager, which applies the samples. |
| **Named REST legs** | The four per-source leg factories (Widgets): each declares its URL shape, wire parse, validation guard, and `SourceLabel` (the price-map freshness guard's discriminator) in one place, `BinanceUsRestLeg` (the crypto cycle's 24hr ticker; no guard, the subscription boundary already validated), `FinnhubRestLeg` (the stock cycle's token-riding quote; the symbol guard), `FrankfurterRestLeg` (the FX series whose date window is built from an injected live clock; the FX-key guard; the empty currency symbol), `YahooChartRestLeg` (the stock one-shot fallback's 1d chart meta; the symbol guard). The manager binds the four through their `Create` factories in its constructor and holds no source label, URL, or parse of its own; `CoinGeckoRestLeg` keeps its own class for its batch shape. |
| **RestPollLoop** | The REST poll cycle (Widgets): the third loop shape beside the Sdk's `PollLoop` (probe + sink on a real-time timer) and `FeedLoop` (WebSocket session with a reconnect policy), delay-first, per-symbol failure isolation (one bad fetch routes through a `DiagLog` and the cycle continues), and an optional batch-tail hook that runs once per cycle after all symbols polled (the crypto cycle's CoinGecko fallback rides it). The subscription set is a LIVE view, read once per cycle after the delay (a `Func<IEnumerable<string>>` the caller re-reads, not a list captured at start): the manager's map owns the membership, the loop only observes it, so a symbol subscribed while the loop was parked is polled on the next cycle and a released symbol stops being polled (a frozen snapshot would starve late symbols, since the loop task never restarts). One cadence constant (`PriceFeedManager.RestInterval`, 30 s) spelled once for all three REST cycles; the delay is an injected delegate, so a fake clock drives the cadence in tests. |
| **IWebSocketFeed** | The WebSocket seam (Widgets) behind the price feeds and the Twitch IRC loop: connect, send text, receive one complete text message per await, abort. `ClientWebSocketFeed` is the production adapter; in-memory fakes drive both consumers' loops in tests, one framing implementation per assembly. |
| **IWidgetIconGrab** | Widget contract for edit-mode icon dragging (Core): the widget owns its icon geometry, hit region, center, and grab-move math, so the input module sees only the capability, never the widget type. Mirrors the `ResizeHandleSize` ownership pattern. Hit-testing runs in the widget's rotated-local space (`PlacedWidgetInstance.ToLocalPoint`, the render-transform inverse), so a rotated icon's grab region matches its drawn footprint. Offsets persist through `ModernWidgetBase.SetProperty`. |
| **TelemetryProducers** | The telemetry producer cluster (App): owns the two direct poll loops (sensor 1s via LibreHardwareService shared memory, frame-time 1s via PresentMon), their producers/readers, and the error-dedup state. The window keeps one `Start()`/`Dispose()`; the cluster is testable without WPF. |
| **LogCadence** | The diagnostic log-cadence rule (Sdk): fires on the first call and/or every Nth, one tested rule replaces the hand-rolled modulo counters that once mirrored each other across the transport, the backend, and the delivery pipeline. |
| **DiagLog** | The category-tagged diagnostic log line (Sdk): composes `LogCadence` (first-log/every-Nth) with `FileLog` and bakes the caller's tag in once at construction, the tag can never drift between call sites. |
| **LogLine** | The log-line rule (Sdk): the single owner of the shape a log value must have before it reaches a line-oriented log, newlines flattened to spaces, credential-shaped query params and URL query strings redacted (the rule `CrashLog` used to own, its `SanitizeMessage` and regexes moved here), and the value bounded to `MaxLineLength` (2000: exception type + message + top frames, never a multi-MB line). `FileLog.Write` applies it once per line (every producer reaches the file through it), `CrashLog.Append` applies it to the exception message, and the widget host sink (`MainWindow.LogInfo`/`LogError`, mirrored by the test context) passes raw values to the sink instead of pre-sanitizing, so a multi-line `ex.ToString()` can no longer corrupt `display_device.log` and the redaction rule has one enforcement point (`LogLineTests` pins flatten/bound/redact; the retired `AppCrashLogTests` pins moved with the rule; `FileLogTests` pins the seam's flatten+redact end-to-end). The per-user-value 200-char cap stays `LogSanitizer`, a different rule (it bounds one user value, not a line, and its call sites don't all route through the sink). |
| **SetProperty / PersistProperty** | The single widget-property bookkeeping invariant (Sdk/App): a widget property lives in both the instance property and the placed instance's `PropertyValues` (the export format). The commit has one owner: `IModernWigiDashContext.SetWidgetProperty` (a default interface member: the instance set + the `OnPropertyChanged` fire + the persistence through `PersistProperty` in one spelling), and the App's `PersistProperty` resolves the owning placed instance by identity and marks the profile dirty. `ModernWidgetBase.SetProperty` (cached property lookup, the once-logged miss) and the inspector's write-back funnel (the re-entrancy guard + the string→CLR conversion through `InspectorValuePolicy`, then the commit) both commit through it, so a new write path that forgets the `PropertyValues` half cannot exist. Pre-initialization (the context not handed yet): the instance still carries the value and the change still fires; the placed half has no owner yet. Every mutation path, inspector write-back, icon-grab moves, widget `OnTouch` toggles, commits through the owner, so export round-trips cannot silently lose runtime toggles (`ModernWidgetBaseTests` pins the commit, the base routing, the pre-init leg, and the missing-property no-op). |
| **FeedLoop** | One WebSocket reconnect-loop shape (Widgets), in the `PollLoop` image: create feed → connect → onConnected → read messages until closed → backoff delay, repeating until cancelled. PriceFeedManager's Binance/Finnhub loops and the `TwitchChatConnection` IRC loop are instances with injected `IReconnectPolicy` (fixed vs exponential backoff) and status/error hooks. |
| **IAudioCaptureSource** | The audio-capture seam behind the visualizer (Widgets): a source of float samples from the system output, drivable by an in-memory fake in tests. `WasapiLoopbackCaptureSource` is the NAudio adapter; the buffer→float conversion is the pure `AudioSampleConverter` (IEEE-float 32, PCM 16/24/32-bit with 24-bit sign extension, pinned by tests); `AudioSpectrumAnalyzer` (pure binning/smoothing) and the gate live behind the thread-safe **AudioFrameBuffer**. The widget renders snapshots; it never touches WASAPI. The capture lifecycle (when a source is alive, how it stops) lives in the **AudioCaptureLifecycle** module, the seam's only consumer: the seam carries only the sample stream (`SamplesAvailable`) and `Start` (+ `Dispose`), and the no-production-caller members (`IsCapturing`, `Stop`) were dropped with the module move. |

### Theme

| Term | Definition |
|------|-----------|
| **ThemeSettings** | Serializable theme definition (Core, display-free): the string-typed color values (`#RRGGBB` hex), the `StringProperties` "themeable = string-typed property" rule, `ParseColor`, and the lazy `app_theme.json` load. The persisted file lives in the USER STATE DIR (`%LOCALAPPDATA%\ModernWigiDash\app_theme.json`, beside `profile.json` and `app_settings.json`, ADR-0021), the one read/write location (`DefaultPath`; the save creates the directory when missing): the former exe-dir location carried a stale-copy hazard (a dev-machine copy in `bin\Release` silently overrode every color until deleted; a stale `AccentGreen` once hid the Connected badge's green dot behind the amber label) and a Debug-vs-Release split, both retired by construction. The exe-dir copy (`LegacyPath`) is a read-only one-time migration source: with the state file absent and a parseable legacy copy present, `Load` carries it across and logs one line (a corrupt legacy copy logs its own line and is not migrated; once the state file exists the exe dir is never consulted, and a corrupt state file degrades to defaults the pre-ADR-0021 way, never repaired from the legacy copy). Every theme-file read (state or legacy) is bounded by the pre-read size cap (`MaxThemeFileBytes`, 1 MB, the pre-read-reject shape the profile-import guard already owns): an over-cap file degrades to defaults with one log line instead of a multi-GB read, and a present-but-JSON-`null` state file (the truncated-write shape) logs one line instead of degrading silently. The theme rides the profile EXPORT bundle as an optional section (ADR-0021, the verified JetBrains/VS Code pattern: `ProfileExportTheme` writes the top-level `"theme"` key into the manual export, the one size-guarded import boundary reads it back into the `Loaded` verdict's `BundledTheme`, and the manual import offers it behind one confirm when `ThemeRestorePolicy.ShouldOffer` says it differs (the pure null + fingerprint gate, pinned at the interface); the persisted profile.json never carries the section, so a hand-edited profile cannot restore a theme, and the profile import itself never touches the theme file, the universal norm across VS Code / JetBrains / Sublime / GIMP / Unity). The harness `wmd-verify.ps1` backup/restore-profile covers both locations (state dir primary, exe-dir copy as `app_theme.exe-dir.json`). The dialog's labels/groups/descriptions no longer live here, they moved to the App-side `ThemePresentation` beside the dialog that renders them, so a display rename can never touch the persisted model. |
| **ThemePresentation** | The theme dialog's display copy (App): the friendly labels, section groups, and per-property descriptions the dialog renders beside each color swatch, the one owner of that copy, pinned to cover every themeable property. The theme draft module (`ThemeDraft`) resolves the copy into per-property entries and the dialog renders from those; the serialized `ThemeSettings` model stays display-free. |
| **ThemeDraft** | The theme dialog's draft module (App): the per-property entries seeded from the active theme (display order group-then-name, the one `"Other"` group fallback, the display copy resolved once through `ThemePresentation`), the single validity verdict (`InvalidEntryName`: the first entry whose hex does not parse, or null; `IsValid` reads it), apply-to-settings (only parseable values are written, so an invalid entry can never corrupt the theme), and reset-to-defaults (a fresh `ThemeSettings`'s values). The entries hold the `PropertyInfo` references, so apply and reset never re-resolve a property by name (the old PropertyInfo → name string → PropertyInfo round-trip is gone). The `ThemeDialog` window builds one editor per entry and forwards: editor changes route through `UpdateHex` into the verdict, the buttons call `ApplyToSettings` / `ResetToDefaults`, and the save + apply + close sequence stays in the window. The decision rules are assertable at the module interface without a window, a Show, or a visual-tree walk (`ThemeDraftTests`). |
| **ThemeManager** | Pushes `ThemeSettings` into WPF application resources. |
| **ThemeRestorePolicy** | The export bundle's theme-restore offer rule (App): `ShouldOffer(bundled, current)` is true only when the bundle carries a theme (non-null) AND its `ThemeApplicator.Fingerprint` differs from the current theme's (the applicator's fingerprint is the one change signal, so a default-theme export never prompts a default-themed machine). The confirm + apply + swap-success ordering stays in the window (the thin adapter); the pure decision is pinned at the interface without a window (`ThemeRestorePolicyTests`). |

### Touch Input

| Term | Definition |
|------|-----------|
| **Hardware touch** | Physical touch from the display, polled by `DisplayDeviceEngine` at 16ms in direct-USB mode, normalized once via `TouchReport.ToEventType`, and raised through `OnTouchEvent` to MainWindow, which enqueues it on the drain queue (one drain scheduled per burst) and feeds it IN ORDER into the input controller, because the gesture machine needs the full ordered Down/Move/Up sequence, not just the last point (the in-order feed is pinned at the window seam, `DeviceTouchDrainTests`). |
| **Widget touch** | Local-coordinate touch events delivered to `IModernWidget.OnTouch()` after compositor hit-testing. |
| **Gesture** | Input-sequence interpretation shared by the USB-direct and mouse paths: one `GestureInterpreter` state machine (swipe 70/80 px for page nav, arrow-tap 60/964 edge zones × 200–400 y-band for page switch, tap for widget touch). The mouse feeds the same Down/Move/Up vocabulary through `InputController.Feed` directly; edit-mode widget manipulation gates the machine in the mouse handlers (widget routing suppressed in edit mode, page actions still applied). |
| **InputController** | The single input module (App) behind which the gesture machine, its outcome application, the routing veto, widget routing, and edit-mode manipulation decisions (drag/resize/icon-grab + snap-to-grid math) all live. Callers only feed `Down/Move/Up` coordinates + the source (Press/Move/Release are source-aware); page-switch UI work stays in MainWindow behind one navigation seam. The desktop edit-mode read is the controller's own injected probe, read per sample and derived from the source, so the veto can't be passed wrong at a call site: the mouse path carries the desktop edit-mode flag (authoring input, presses manipulate), the physical display never does (runtime input, hotkeys fire on the device even in edit mode), and the per-sample derivation (never cached at construction) is pinned by test (`InputControllerTests`). |

### Window Lifecycle & Tray

| Term | Definition |
|------|-----------|
| **CloseBehaviorPolicy** | The close-behavior vocabulary and parse rule (Core/Models, ADR-0018): the one owner of what the persisted `ProfileLayout.CloseBehavior` value may name. The profile is a hand-editable traveling artifact, so the value is a raw string (the `ThemeSettings` precedent) and this policy is the single parse site: `Quit` ("quit", the pre-feature behavior: close, minimize, and Alt+F4 all run the full teardown) and `HideToTray` ("hideToTray": the window hides, the display stays live, and the tray menu's Quit is the only exit). `IsKnown` is the exact spelling and case is identity (a hand-edited "QUIT" is unknown, not quit-with-a-typo), `Resolve` degrades null/whitespace/unknown to `Default` (which is `Quit`, so a legacy or hand-edited profile can never smuggle in a hide), and `MergeImport` is the import merge (an imported profile lacking the field takes the local value, resolved first, so the stamped value is always a known spelling the next export can carry; a present imported value wins) with the window's import handler as its one caller. The untrusted-import rule sits beside it in `ProfileImportSanitizer` (absent stays absent, present-but-corrupt normalizes to `Quit`). |
| **CloseInterceptPolicy** | The window's close-intercept decision (App, ADR-0018): the one predicate `ShouldHide(persistedBehavior, trayLive)` serving both the window-close (X, Alt+F4) and the minimize intercept, so the two can never drift. The persisted value routes through `CloseBehaviorPolicy.Resolve` (a hand-edited profile can never smuggle in a hide), and the tray icon must be live: with no tray the user would have no way back to the window, so the action falls through to the normal behavior (the N1 fallback: a broken tray degrades to the pre-feature close instead of hiding into a void). The window's `OnWindowClosing` and `OnWindowStateChanged` (minimize, M2) read the tray liveness from `TrayIconController.IsLive`, and only the tray menu's "Quit" (`QuitFromTray`/`QuitClose`) runs the real close. |
| **SingleInstanceGuard** | The per-session single-instance claim (App, ADR-0018): a named mutex (`ModernWigiDash.SingleInstance`) decides primary vs secondary, and a manual-reset activation event (`ModernWigiDash.Activate`) carries the secondary's signal. The primary creates the event and parks a one-shot `RegisterWaitForSingleObject` (re-parked BEFORE firing, so a fast second secondary launch cannot lose its activation; the manual-reset signal is consumed and handed to the window's wiring, which hops to its dispatcher and restores the hidden window through `ShowFromTray`). The secondary opens the event, signals it, logs one line, and exits (a vanished event, the primary having died between claiming the mutex and creating the event, is logged, not fatal: this launch exits either way). The handle factories are injected through the `GuardHandleFactory` record (the test seam), so the verdict and signal policy run against in-memory handles without touching the OS, and `Dispose` is idempotent (unregister + event + mutex). |
| **TrayIconController** | The notification-area icon's policy module (App, ADR-0018): owns the icon's show/hide lifecycle, routes the surface's click and menu events to the injected show/quit delegates (the one routing spelling; the production adapter is a thin WinForms `NotifyIcon` binding behind the `ITrayIconSurface` seam, `NotifyIconTraySurface`, and the tests bind an in-memory fake), and exposes `IsLive`, the N1 guard the close path reads (false before `Start` and after `Dispose`, so a close path never sees a stale "live" after teardown removed the icon). The icon is always present (created on the `Tray` wiring step regardless of the close behavior), the menu is `TrayMenu.Default()` (Show / separator / Quit; the separator arm exists so an added command can never silently no-op), and `Start` is idempotent (a second Start would double-fire the show on one click). The verdict line reads the SAME live state the N1 guard reads, so the log can never claim an icon the close path would not trust (the 2026-08-25 trap: the ico was missing from the output, `Show()` refused to bring the icon up, and an unconditional "icon shown" line hid the dead tray). `Dispose` is idempotent and safe to call without a `Start`; the teardown plan's `TrayDispose` step routes through here, so the icon is gone before the process exits and a force-killed test host never leaves a ghost icon. |
| **Settings hub** | The grouped settings dialog (App/Dialogs, ADR-0018): the title-bar "⚙️ Settings" button (which used to open the theme dialog directly) opens a 460×560 hub with the three groups in display order (`SettingsModel.Groups`: Appearance, Behavior, Profile). The Behavior group's close-behavior radios (`SettingsModel.CloseBehaviors`: the policy's constants as values, never display strings the dialog would have to re-map, the pre-feature exit first) seed from the persisted value through `CheckedCloseBehaviorFor` (routed through `CloseBehaviorPolicy.Resolve`, so a null/whitespace/hand-edited value lands on the default radio) and write through on selection change into the window's `CommitCloseBehavior`, so the profile is dirty at the moment the user picks, not at dialog close. The Appearance group opens the theme editor (the `ThemeDialog` content) and hosts the page-background picker (the active page's canvas color, relocated from the page-tab strip, write-through on pick), and the Profile group's Export/Import buttons route into the window's file seams. `SettingsModel` is the decision model, pure and WPF-free, and the dialog builds its rows from it the way `ThemeDialog` builds from `ThemeDraft` (`SettingsModelTests`, `SettingsDialogTests`). |
| **AutostartPolicy / StartupLaunchPolicy / IAutostartStore** | The Start-with-Windows shape (App, ADR-0019): the one HKCU Run entry the app owns (`ModernWigiDash` under `Software\Microsoft\Windows\CurrentVersion\Run`), the command line (the quoted exe path + the `--startup` flag; the quoting is unconditional, so a bare and a quoted path cannot drift), and the read side at process start (the case-insensitive flag parse in `App.OnStartup` - one spelling owner the writer and the reader share). The registry is the single source of truth: the settings checkbox reads the entry's presence and the toggle commits through `SetCommandLine` (null deletes) with no Apply step, so the choice is machine-local by construction and a profile import can never overwrite it. HKCU, not HKLM (per-user, no elevation; the vendor-verified shape). The autostarted instance opens the window minimized, not hidden (the frames stream), a one-shot latch keeps the minimize-to-tray intercept (ADR-0018 M2) from swallowing the login-time minimize, and a second launch is already covered by the `SingleInstanceGuard` (it signals the first, which comes forward). The entry is a snapshot at toggle time and is not self-healing across an exe swap (the vendor-identical edge); a re-toggle rewrites the path. `RegistryAutostartStore` is the production adapter; tests round-trip the real adapter through a temp HKCU subkey (the `TwitchTokenStore` real-DPAPI precedent) or inject an in-memory fake. Pinned by `AutostartPolicyTests` / `StartupLaunchPolicyTests` / `RegistryAutostartStoreTests` / `WindowAutostartTests` (the live STA window's toggle commit + the `--startup` shape). |
| **AppSettings / AppSettingsStore** | The machine-local app settings (App, ADR-0019): `app_settings.json` beside `profile.json` in %LOCALAPPDATA%\ModernWigiDash, holding the global-hotkey integration's `KillSwitch` and the user's `AhkInterpreterPath`. Deliberately outside the profile: importing someone's profile can never overwrite the machine-local choice. A corrupt or absent file repairs to the defaults with one log line (the absent-service house pattern), the save is atomic (tmp + replace, no `.tmp` residue), the path is injectable for tests (the LocalAppData default in production), and the window holds the live value (reloaded at the AppSettings wiring step, mutated in place by the settings hub's write-through commits), read live at spawn time, so a write-through is seen at the next action without a restart. Pinned by `AppSettingsTests` (the round-trip, absent → defaults, corrupt → defaults + one log line, the atomic save, and the save creating the directory). |
| **KillSwitch** | The global-hotkey integration's off switch (`AppSettings.KillSwitch`, ADR-0019): the settings hub's checkbox, default unchecked = live (the vendor parity); checked = kill, scoped to the OS-visible surface only: no global-hotkey registration (a tripped toggle unregisters the profile's chords through the idempotent refresh pass) and the AHK spawn is refused with a log line even from a tap; every other action (Launch, URL, media, page flip) keeps running from a tap. Rationale: some games flag the background listener and the spawned script process as cheat software, and the user wants one off switch for all of the integration. Machine-local (it lives in `app_settings.json`, never the profile), so it survives profile travel: an import cannot re-enable what the user killed on this machine. Pinned by the registration resolution (`GlobalHotkeyPolicyTests`: the tripped state vetoes the whole candidate set), the spawn refusals (`WindowAhkScriptTests`), and the settings write-through (`SettingsDialogTests`). |
| **GlobalHotkeyManager / HotkeyApi / IGlobalHotkeyProvider** | The global-hotkey integration's OS boundary (App/Hotkey + Widgets, ADR-0019): the hotkey widget's `Global Hotkey` property (empty = none; edited with the inspector's key-capture editor, `KeyCaptureModel` - the pure decision model the thin WPF mapper drives, the `LocationSearchModel` precedent, reached through `EditorKind.KeyCapture`) stores a chord in the same plus-separated shape the executor's `ParseVirtualKey` already validates, so the chord vocabulary has one owner. The App discovers hotkey-capable widgets through the optional `IGlobalHotkeyProvider` (Widgets; the `IWidgetEditorProvider` precedent, no concrete-type `typeof` checks): `TryGetGlobalHotkey` (the stored chord parsed into its `RegisterHotKey` operands through `GlobalHotkeyChordPolicy`) and `FireGlobalHotkey` (the widget's single fire path: the re-entrancy gate, the 30-second timeout, and the failure logging are shared by the touch-up and the hotkey trigger). `HotkeyApi` is the delegate bag over `RegisterHotKey`/`UnregisterHotKey` (the `WinUsbApi` house pattern; the P/Invoke acquires no handle; the entry points are spelled explicitly and probed against the real DLL by `PInvokeBindingTests` (ADR-0020) - the 2026-08-26 crash threw EntryPointNotFoundException on the first real registration because the binding resolved to the method names, and every test injects a fake, so `HotkeyApiTests` additionally invokes the production delegates with the degenerate zero chord, the return value machine-defined and unasserted) on the main window handle - a hidden or minimized window keeps pumping its message loop, so hotkeys work while the app is tray-hidden. `GlobalHotkeyManager` owns the registration state: the ids are stable across refreshes, the refresh is a fully idempotent diff against the OS (triggers: profile load, a widget placed/removed, a chord edit, a kill switch toggle, an interpreter path change), a duplicate chord is won by the first widget in profile order (the later ones stay tap-only + one log line), and a chord owned by another program is untracked + one log line per cell per session. Pinned by `GlobalHotkeyChordPolicyTests` (the vocabulary + vetoes without the OS), `GlobalHotkeyPolicyTests` (the resolution policy), `GlobalHotkeyManagerTests` (the diff, stable ids, and foreign cells through the injected api), and `WindowNavigatePageTests` (the WM_HOTKEY end-to-end on a live STA window through the injected api, including the page flip). |
| **GlobalHotkeyChordPolicy** | The chord vocabulary (Widgets, ADR-0019): parses the stored chord into the `RegisterHotKey` operands. The rules: at least one modifier and exactly one main key (a modifier-less chord would shadow the key system-wide; a modifier-only chord names no key), at most one occurrence of each modifier, and an unknown key is unparseable (the main key's name resolves through `HotkeyActionExecutor.ParseVirtualKey`, so the key-name table has one owner). The Win32 MOD constants (`ModAlt`/`ModControl`/`ModShift`/`ModWin`) and `ModNoRepeat` (always rides the registration at the OS boundary) are declared here. `GlobalHotkeyChordPolicyTests` pins the composition, the case/spelling variants, the function/digit keys, and every refusal. |
| **AhkLaunchApi / LaunchAutoHotkeyScript** | The user-supplied AutoHotkey spawn (App/Hotkey + the Sdk seam, ADR-0019): the "Run AHK Script" hotkey action (the catalog's `AhkScript` kind; `NeedsCommand` - the command is the `.ahk` path, edited with the existing Path editor) routes through the context seam `IModernWigiDashContext.LaunchAutoHotkeyScript` (default no-op, the `NavigatePage` precedent; the fire path places the routing after the empty-command skip, so a blank script path never reaches the seam) instead of the SendInput executor. The window resolves the interpreter live from `app_settings.json` at spawn time: a checked kill switch, a blank path, and a missing exe each refuse with one log line (the absent-service house pattern: an observable no-op, never a throw); on success the `AhkLaunchApi` delegate bag (the `HotkeyApi` precedent; production binds the `Process.Start` bare spawn, tests bind a recorder) launches the user's `autohotkey.exe` with the script as the argument - a fresh interpreter per trigger, no tracking, nothing bundled or auto-detected. The vendor's embedded AHK v1 engine (inside the x86 service) is deliberately not ported: the app is 64-bit and service-less (ADR-0005), and the scripts run outside our process and cannot drive the display (no IPC surface added; the display control the hotkey actions have is the built-in page-nav only). Pinned by `WindowAhkScriptTests` (the kill-switch veto, the unset/missing refusals, and the spawn-once success path on a live STA window) and the widget's routing pins (`HotkeyButtonWidgetTests`). |

## Architecture Overview

```
┌─────────────────────────────────────────────────────────┐
│  ModernWigiDash.App (WPF host)                          │
│  MainWindow + partials (Context, Update)                 │
│  Compositor → RGB565 → FrameDelivery → direct USB        │
│  Touch: engine 16ms poll → gesture detection → widgets   │
│  Producers: LHS shared memory (sensors), PresentMon      │
└──────────────────┬──────────────────────────────────────┘
                   │ Direct USB (WinUSB)
┌──────────────────▼──────────────────────────────────────┐
│  ModernWigiDash.Hardware                                │
│  DisplayHidTransport (USB HID) + DisplayDeviceEngine    │
│  Transport/ (FrameEncoder, TouchReport, WinUSB, SetupAPI)│
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ModernWigiDash.Widgets                                 │
│  12 widget implementations                              │
│  Stores (LhmSensorStore, FrameTimeStore)                │
│  PriceFeedManager, Twitch, TextRenderHelper              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ModernWigiDash.Core                                    │
│  Compositor, Theming, Rendering, Plugins, Models         │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  ModernWigiDash.Sdk                                     │
│  Contracts (IModernWidget, IModernWigiDashContext)       │
│  Attributes (WidgetMetadata, WidgetProperty)            │
│  Data models (SensorSnapshotDto, FrameTimeSnapshotDto)  │
│  FrameDelivery, PollLoop, FileLog                       │
└─────────────────────────────────────────────────────────┘
```

Reference edges (one owner: the `ArchitectureTests` allowlist, machine-pinned
against the csproj files in the same shape; dependency direction is inward,
Sdk the leaf, App the top): Core → Sdk; Hardware → Sdk; Widgets → Core, Sdk;
App → Core, Hardware, Sdk, Widgets; Tests → all five.

### Data Flow

1. **Render loop**: MainWindow tick → `SkiaFrameCompositor.Compose()` → `FrameDelivery` (encode + pool + pace) → `DisplayDeviceEngine.SendFrameBytes()` → `IDisplayTransport.SendFrame()`
2. **Touch flow**: `DisplayDeviceEngine.TouchPollTick` (16ms PollLoop, reads `ReadTouch()`, normalizes raw byte → `TouchEventType` via `TouchReport.ToEventType`) → `OnTouchEvent` → MainWindow → `SkiaFrameCompositor.RouteTouch()` → `IModernWidget.OnTouch()`
3. **Sensor flow**: `LhmSharedMemoryReader` (App, reads LibreHardwareService shared memory on the 1s `PollLoop` shape) → `LhmSensorStore.UpdateFromDto()` (static cache) → `HardwareMonitorWidget.Render()` reads snapshot
4. **Frame-time flow**: PresentMon producer (ADR-0003: `pmOpenSession` → `pmStartTrackingProcess(pid)` → dynamic-query poll on the 1s `PollLoop` shape) → `FrameTimeStore.Update()` (static cache) → `FrameTimeWidget.Render()` reads snapshot

### Key Design Decisions

| Decision | Rationale |
|----------|-----------|
| Synchronous transport interface | USB I/O (WinUSB/LibUsbDotNet) is inherently blocking; fake async adds no value and forces sync-over-async bridges |
| Static stores with LastUpdate freshness | Widgets are instantiated via reflection (parameterless ctor) and can't receive injected dependencies; static stores with staleness tracking are the pragmatic solution |
| Telemetry DTOs in Sdk | `SensorSnapshotDto`/`SensorReadingDto`/`FrameTimeSnapshotDto` live in the lowest common layer so every project shares one mailbox format (the Service.Contracts assembly was removed with the service, ADR-0005) |
| FileLog in Sdk | Shared file logging utility placed in the lowest common layer so all projects can log to `display_device.log` without circular dependencies; its `RotationCapBytes` is the one shared rotation bound (the App's crash log rotates at the same cap). Writes are cadence-flushed (8 KB / 250 ms) and the App's `Exit` handler does the one final `Flush`, so the last lines, including the shutdown standby verdict, always land at exit. `FileLog.Write` is the one line-policy owner (it applies `LogLine.Sanitize` once per line, so every producer lands a well-formed line without spelling the rule) |
| Close behavior is an opt-in profile setting (ADR-0018) | The pre-feature teardown stays the default: the behavior persists per profile, a hand-edited or corrupt value can never smuggle in a hide, and the tray's liveness is a required operand of the intercept, so a broken tray degrades to the old behavior |
| Widget-per-file convention | Each widget class lives in its own `.cs` file with its `[WidgetMetadata]` attribute; the catalog is discovered by scanning the assembly. One type per file also holds for the Sdk contract, `IModernWidget`, `ModernWidgetBase`, and `IWidgetActionInvoker` each own one file (split out of the former single `IModernWidget.cs`) |

### Testing

- 2025 unit tests covering protocol framing, RGB565 encoding, DTO mapping, telemetry store freshness, touch routing/normalization, frame delivery/coalescing (including the tri-state send seam's refusal-vs-failure split, the drop accounting's reason split under the `DroppedCount` total, and the first drop/refusal lines through the log seam), widget property defaults + persistence round-trips, price-feed lifecycle and WebSocket-seam behavior (including the per-kind subscription wiring table, one shared routine: validate → ref-counted claim → first-claim start → WS subscribe → the seed leg as a table column, and the subscribe frames' exact wire bytes through the feed seam: Binance's one frame with every stream key and Finnhub's one frame per symbol, the connect-time and incremental sends pinned against the `PriceFeedMessages` builders), the REST quote legs through the HttpClient seam (the four named source legs' URL shapes and label pins, validation guards, unparseable-response rejection, the CoinGecko leg's id resolution and distinct-id batching against the SymbolCatalog table, and the one-shot fallback seed's downgrade guard (a fresh live price survives the seed, from the Binance US REST, the Binance WebSocket, or the Finnhub stock feed, while a stale one is replaced, and FX makes no request)) and the REST poll cycle on a fake clock (delay-first cadence, per-symbol failure isolation, batch-tail ordering, the live membership view, and the manager-level pin that a late FX pair subscribed after the loop started is polled on the next cycle through the Frankfurter seam), the price-map store seam (`PriceMapStore`: the live rule's always-overwrite + change-keep for a sample with no change figure, the fallback rule's downgrade guard + change-keep, the read/release seams, and the one clock, `PriceMapStoreTests`), the hardware monitor's display rules (`SystemTelemetryPresentation`), the picture widget's source-resolution policy (`PictureSourcePolicy`), the Location-search editor rules (`LocationSearchModel`), the icon-value policy (`IconValuePolicy`: the named/custom verdict, the file-wins read precedence, the mutual-exclusion write-back), the sparkline geometry module (`SparklineRenderer`) and the word-wrap rule pinned with its cache (`WrapCacheTests`), the font cache overflow-bound rule (the exclusive boundary + the whole-cache reset, `FontCacheEvictionTests`), the close-budget derivation (`CloseBudgetPolicy`: the cap-vs-half-bound crossover, the strict-shorter-than-bound sweep, the engine/transport same-value pin, `CloseBudgetPolicyTests`), the media-decode module (`MediaDecoder`: the bomb caps' refusals, the delta-composite pixel pins, the single-frame still routing, the never-throw + dispose-partial contract, `MediaDecoderTests` on binary GIF fixtures), the named capture window (`CaptureWindowGuard`: the ADR-0006 predicate at every site, the `Dropped`/`StillCurrent` verdicts, `CaptureWindowGuardTests`), transport policy through the ITransferBackend seam (including the connect fallback policy, the init verdict's blank-frame bulk-write pins (a short or refused init write fails init; the connect falls back on a failed init bulk write), standby, and the CloseBound-vs-close-budget invariant pinned on both sides of the transport/engine seam), FramePump cadence, IRC-loop behavior through the feed seam (handshake, PING→PONG echo with the same payload, reconnect backoff, PRIVMSG parsing), the Twitch device-token poll loop (interval growth, slow_down, timeout/error expiry) through the HttpClient/Clock seams, and the session validation tick (the restamp/refresh/clear verdicts, the hung-validation gate pin, the network runs outside the user gate, the stale-verdict-after-logout pin, and the device flow's trusted-open seam, the test ctor's recorder observing the one verification-page open and a lookalike-host URL never reaching it, `TwitchSessionTests`), audio DSP + capture lifecycle (the `AudioCaptureLifecycle` module at its interface: the start-on-render / stop-on-stale policy, the fresh-capture watchdog prime, the stale-render deferred stop on a fake clock, the page-switch-back re-arm, and the start-failure retry (a half-opened source is disposed, the failure logs once, and the next render tick retries instead of wedging)), the media-session seam's neutral vocabulary + tap-action policy (the toggles invert the monitor's live state, not a caller-held snapshot; the capability vetoes settle; the repeat cycle steps from live state; the seek ratio resolves against the live duration; and the widget-level not-shuffleable veto rides the monitor-factory seam, `MediaSessionMonitorTests` / `NowPlayingWidgetLiveSessionTests`), icon-grab geometry, SVG-path draw scaling + cache fallback/keying (`SvgIconHelper`), page-tab rules, the tab-strip seams, wheel-scroll inversion and scroll-into-view (`PageTabsView`), telemetry-producer ticks, cached color parsing, stopwatch timing, feed subscription lifecycle, log-cadence and log-on-change rules, the icon picker's decision model (`IconPickerModel`: the case-insensitive filter, the selection + highlight, the chip that follows the selection, and the accept verdict), the device-authorization decision model (`DeviceAuthorizationModel`: the trusted-open verdict (a lookalike host and a file scheme never reach the shell-open seam), the copy verdict, and the slot-clear identity rule), dialog hosts, inspector write-back (the re-entrancy guard at the write-back funnel, the per-fired-box transform parse, through the two facet bindings) and the property commit owner (`SetWidgetProperty`: the instance + fire + placed persistence in one spelling, the base's routing through it, the pre-initialization leg, and the funnel's commit, `ModernWidgetBaseTests`), the window's one post-mutation contract (`ProfileMutationShape` refresh bundles + the single dirty mark reaching the profile file), the widget default-size contract (`DefaultGridSize` on [WidgetMetadata]: the catalog's nominal size agrees with the instance's DefaultSize, and centered placement reads the catalog without constructing a probe), the import-time size fallback (a size omitted from hand-crafted JSON rehydrates at the widget's declared preset, explicit sizes win, `ProfileOpsTests`), profile-import sanitization caps (including null collections) plus the sanitizer's pure rule pins (`ProfileImportSanitizerTests`: the InstanceId safety rule, the file-size guard boundary), the file-import boundary's named verdicts (`ProfileOps.ImportProfileFile`: the oversized-before-read rejection at the cap's exclusive boundary, the read-then-unparseable `Failed` verdict, the trusted/untrusted split at the boundary, and the bundle's optional theme section extracted from the one size-guarded read into the `Loaded` verdict, `ProfileOpsTests`), present-mon blob parsing, the input press orchestration (source-aware Press/Move/Release) and the window's device-touch drain (the in-order feed into the gesture machine: an in-order swipe burst switches the page, a release without its contact is not a gesture, and one drain feeds every event of a burst, `DeviceTouchDrainTests`), the update flow (the pure flow state machine's check/download/failure transitions + click decision + the single tooltip spellings (`UpdateFlowTests`), plus the checker/service/script through their seams), the teardown standby guarantee and per-step isolation (`ShutdownOrchestrator`: a throwing step logs one line and the plan continues, so a long-lived host never inherits the modules an aborted tail would have left undisposed, `ShutdownOrchestratorTests`) and the `TeardownPlan` shape pins (persist-first and precedes-own-dispose, the pump before the delivery, the engine standby strictly last-resort and never ordered, and the exit marker landing in the log, `TeardownPlanTests`), the widget memo pattern (`MemoSlot`), the fallback-seed cadence (`TickerFallbackPolicy`), the display-rules culture contract (`DisplayFormat`), the shell-open trust policy (`TrustedUriPolicy`), the widget-creation outcome policy (`WidgetCreateResult`), the routing module (`WidgetRouting`), the IRC status policy (`TwitchChatStatusPolicy`), the location-resolution rules (`WeatherLocationResolverTests`: the suffix/alias/abbreviation tiers, the postal-routing battery, the exact-name predicate + the persisted-pick exact-name promotion, and the count=100 search pin, `TrustedUriPolicyTests`, `DisplayFormatTests`), the weather fetch-outcome union (`WeatherFetchResult`, including the tie arm that carries the tied candidates to the pick list, ADR-0009), the response-aware weak-ISO fallback, the shared log-sanitization rule (`LogSanitizer`), the log-line rule (`LogLine`: flatten/bound/redact; the redaction pins moved from the retired `AppCrashLogTests`), the profile page-range invariant (`ProfileLayoutTests`: the `Pages` setter repair + the total `ActivePage`), and the window's page-delete seam (the module's bounds-safe `TryGetPage` makes a stale index a silent no-op, never a throw ahead of `DeletePage`'s validation), the weather fetch-flow module (`WeatherFetchFlow`: the capture-window order, the single post-await drop gate through the ADR-0006 predicate, covering the tie's `Tie` outcome too, the drop-and-refetch routing, the write-back gating, the tie apply (the tied candidates population + the placeholder reset, ADR-0009), the cadence gate, throttle window / force / static-snapshot veto, and the boot-load rollback, `WeatherFetchFlowTests`), the twin invalidation rule and its twin-equivalence pin (`WeatherInvalidation`, `WeatherInvalidationTests`), the theme dialog's display copy (`ThemePresentation`, keeping the serializable model display-free) and the theme draft module (`ThemeDraft`: the seeded entries in display order, the single validity verdict, the parse-only apply, and the reset to defaults, `ThemeDraftTests`), and the weather/twitch/theme layout & message modules (`WeatherGeocoder`, `WeatherLocationResolver`, `WeatherLayout`, `WeatherWidgetRenderer`, `TwitchIrcMessages`, `SymbolCatalog`, `ThemeApplicator`, `WrapCache`), and the manager-parity cluster (ADR-0019): the HKCU autostart shape (the command line's unconditional quoting, the case-insensitive flag parse, the registry round-trip through a temp HKCU subkey, the `--startup` minimized shape on a live STA window), the clock seconds' display facts (the `ShowSeconds` `hh:mm:ss`/`HH:mm:ss` variants, the untouched AM/PM + date badge + analog), the weather Hide Location's visibility rule (the blank-title verdict over the key's fields, the CustomLabel + guidance lines that keep rendering, the deleted custom-label confirmation subtitle), the global-hotkey chord vocabulary (the MOD composition, the modifier + one-main-key vetoes, the case/spelling variants, the function/digit keys), the registration resolution + idempotent diff (the stable ids, the first-widget-wins duplicate drop, the foreign-owned untracked cell + its one log line), the key-capture editor rules (the house spelling, the no-modifier / modifier-as-main / blank vetoes, the armed-until-accepted capture), the machine-local settings store (the repair-to-defaults with one log line, the atomic save), the AHK spawn policy (the kill-switch veto, the unset/missing-interpreter refusals, the spawn-once success through the injected api), and the page-nav context seam (the `SetActivePageIndex` clamp without a wrap, the WM_HOTKEY end-to-end through the injected api)
- `DisplayProtocolTests`: widget config layout + RGB565 encoding (BGRA framebuffer format)
- `ArchitectureTests`: the repo's architecture and house-rule pins, driven against the repo tree through an `AssemblyMetadata` repo root (no build or process needed): the documented layering edges (every csproj `ProjectReference` is one of the `ArchitectureTests` allowlist's edges, the machine-pinned form of the Architecture Overview's reference-edges line, and the slnx holds exactly the six projects), ADR-0001's synchronous transport seam (no async members and no awaitable leaks on `IDisplayTransport` / `ITransferBackend` / `DisplayHidTransport` and the backends; async is detected via a Task-like return or `AsyncMethodBuilderAttribute`, because this machine's BCL lacks `MethodBody.IsAsync`), one widget per file + unique catalog ids, one Sdk contract per file, no ambient clock in src (`DateTime.Now` / `UtcNow` is Tests-only), no ad-hoc `new HttpClient` in src (the one allowed site is the manager's shared process-wide client), no empty catch in src (raw scan: the two documented best-effort catches pass as-is), and the `DisplayGeometry` hardware pixel-area pin (1016 x 592 x 2 bpp) read through reflection `ConstValue` so the pin cannot fold into the constant and become tautological. Both pins are negative-verified (an injected violation fails with its coaching line); the raw-scan mechanics (`RepoScan`: the `AssemblyMetadata` repo root, the src-project and Tests-project lists, the comment/string/raw-string-literal strip) are shared with `DebtGuardTests` so the pins police the same text
- `DebtGuardTests`: the mechanical debt guardrails (inadvertent debt enters when standards drift or a review misses a subtle anti-pattern, so the mechanical layer runs in the gate's test stage and the commit guard enforces it before every commit), pinned against the src tree (and the Tests tree for the window-constructor rule) with the shared `RepoScan` raw scan. The strip runs two variants of equal length (same indices): the full strip for declarations and structural spans (a name inside a string literal is not a declaration), and a comment-only strip for occurrence scans (interpolation holes are code, a call inside `$"...{Call()}..."` is a real use, and a name inside a plain string only ever makes the pin conservative). The rules: sync-over-async only at the documented budgeted sites (`.Wait()` / `.Result` / `GetAwaiter().GetResult()` against a per-file allow-list with a reason, drift-checked both directions, `Monitor.Wait` / `Thread.Wait` excluded as thread joins: the engine's standby verdict + dispose-abandon budget + connect-fault continuation, the profile load's synchronous widget InitializeAsync, the FrameDelivery/PollLoop/FeedLoop bounded stop waits), `async void` only on the event-handler shape (the `EventArgs` parameter), handle-acquiring files carry their disposal evidence (a P/Invoke extern / MemoryMappedFile / UsbContext / named mutex / loaded native library must show `IDisposable` / a dispose member / a `using`-scoped release / a release-API call in the same file, or a documented exception: the DWM window-attribute call that owns no handle, the process-lifetime PresentMon service DLL deliberately not freed, the SendInput extern that acquires nothing, the RegisterHotKey/UnregisterHotKey extern whose registration the owning manager releases), the frame pipeline's encode + buffer pool have one entry (`.Encode(` only in `FrameDelivery`, `FrameBufferPool` only in the delivery and its own file: a second site re-creates the per-frame LOH churn the pool kills), and no dead private helpers (a private method with no call site in its type's files or the project XAML, transitive chains swept; reachability runs from external sites plus the XAML-wired handlers, so the helpers a handler-only chain calls stay alive), and every P/Invoke in src spells its entry point explicitly (the method-name binding is a rename away from the first-call EntryPointNotFoundException; the spelled pairs are probed against the real DLL by `PInvokeBindingTests`, ADR-0020), and every window test construction binds an inert USB engine (each spelled `new MainWindow(` site in the Tests tree must carry `FakeTransport.InertEngine` or an explicit `DisplayDeviceEngine(`: the window's constructor starts its engine - the connect probe, the 16 ms touch poll, the 5 s reconnect timer, the teardown standby - so a bare construction would wake, init, and put to sleep the user's attached display on every test run; the short test ctors are deleted, so the spelled form is what the pin polices). The shared strip's raw-string-literal handling is pinned the same way (a `"""` body in scanned source is blanked wholesale, the possibly-longer closing quote run is consumed with the literal, and the strings after it keep their pairing - without it, the injected-violation snippet's own raw string would corrupt the pin's scan of the file that hosts it). Every rule is negative-verified (an injected violation fails with its coaching line); a new legitimate site is a deliberate allow-list edit with a reason, and the judgment layer (is the reason true, is the abstraction earning its place) is the code-reviewer agent's debt dimensions on branch review
- `DisplayDeviceEngineTests`: direct-USB touch polling (normalized events, null-report skip), touch type normalization, protocol constants, and dispose safety (the close budgets now read the transport's `CloseBudgets`, the CloseBudgetPolicy derivation of its CloseBound, and the one bounded-wait routine's throw verdict pinned at both standby entry points through the FakeTransport's `GoToStandbyFailure` seam)
- `ProfileMutationContractTests`: the window's `ApplyProfileMutation` contract on a live STA window: each `ProfileMutationShape` applies exactly its refresh bundle (structural rebuilds the tab strip, transform touches nothing structural, raw-write rebuilds the tab strip and re-syncs the snap toggle from the imported page), one window-driven dirty mark reaches `profile.json` through the debounce, and the page-delete seam turns a stale index into a silent no-op rather than a throw
- `TeardownPlanTests`: the teardown plan's shape on a live STA window: persist-first and precedes-own-dispose (the profile flush before the compositor the profile reads), the frame pump before the frame delivery, the USB-engine standby strictly the last resort and never ordered, and the exit marker + final log flush landing in the log
- `StartupWiringTests`: the startup wiring's shape on a live STA window: persistence + the host modules before the profile load (a widget's InitializeAsync calls back into the context), the frame pump after the modules it composes into, the state resyncs before the wired arm, the wired arm strictly last, and the step-name uniqueness pin
- `CloseBehaviorPolicyTests`: the persisted close-behavior vocabulary pinned without a window: `IsKnown` over the exact spellings (case is identity: a hand-edited "QUIT" is unknown, not quit-with-a-typo), `Resolve` degrading null/whitespace/unknown to the default, the `Default`-is-Quit pin, and `MergeImport`'s absent-takes-the-local (resolved first, so the stamped value is always a known spelling) and present-wins rules
- `CloseInterceptPolicyTests`: the one close-intercept predicate pinned without a window: hide only when the resolved behavior is `hideToTray` AND the tray is live, and every other combination (quit behavior, unknown behavior, dead tray) falls through to the normal close (the N1 fallback)
- `SingleInstanceGuardTests`: the per-session single-instance claim pinned through the injected `GuardHandleFactory` handle record: the primary claim, the secondary verdict + signal, the mid-startup signal not lost (the manual-reset event stays set across the one-shot re-park), the two-fast-signals no-loss rule (the re-park-before-reset ordering can over-deliver a fast second signal, a benign race, so the count is pinned `>= 2`, never the exactly-once 2: two real OS event + thread-pool round trips are load-sensitive, and the shared CI runner starved the thread pool past the 30 s and 60 s ceilings on 2026-08-26 and 2026-08-27, so the CI workflow runs this test alone in a fresh test host with a bounded 3-attempt retry while the other 2024 tests run once, unmasked), and the idempotent dispose
- `TrayIconControllerTests`: the always-present tray module pinned through the `ITrayIconSurface` seam: the menu's default Show/separator/Quit shape, the single-click and menu routing into the show/quit delegates, the liveness tracking (false before `Start`, true while shown, false after `Dispose`), the second-Start no-op, the idempotent dispose, and the honest verdict pin (a dead surface logs the "icon NOT shown" line, never a claimed icon)
- `SettingsModelTests`: the settings hub's decision model pinned without WPF: the three groups in display order (Appearance/Behavior/Profile), the close-behavior entries in display order over the policy's constants (never display strings), and the checked-radio verdict (a null/whitespace/hand-edited value lands on the default radio)
- `SettingsDialogTests`: the settings hub's dialog pinned on an STA window: the radio seeds from the committed behavior without committing, a radio change writes through to the `CommitCloseBehavior` seam, the Profile group's Export/Import buttons route into the window's file seams, the Appearance group exposes the theme editor and the page-background picker (the active page's canvas color, relocated from the page-tab strip; seeds from the page's persisted background without committing and writes through the `CommitPageBackground` seam), the Start-with-Windows checkbox seeds from the registry-backed store without committing and writes through the `CommitAutostart` seam, and the Behavior group's kill switch + AutoHotkey path rows seed without committing and write through the `CommitKillSwitch`/`CommitAhkInterpreter` seams (the Browse button routes into the file-dialog seam)
- `MainWindowConstructionTests`: the window's construction on a live STA thread (title, the XAML-init order NRE guard) and the USB engine seam: a window constructed with an injected fake-transport engine routes the no-probe session-end standby through that engine (the verdict rides back from the fake transport's canned answer, so the test host's window drives the injected engine and never the user's attached display)
- `WindowCloseInterceptTests`: the window's close intercept on a live STA window: a close (X) and a minimize both hide (the window is hidden, the process stays alive) when `hideToTray` is committed and the tray is live, `QuitClose` still exits, and a quit behavior runs the normal close, plus the N1 fallback at the window level (behavior on + dead tray runs the normal close instead of hiding into a void), the minimized-owner veto (a system-wide minimize with a modal dialog open, the disabled owner, does not hide), and the tray-restore state rule (the close leg keeps a maximized window maximized, the minimize leg forces Normal back)
- `AutostartPolicyTests` / `StartupLaunchPolicyTests`: the Start-with-Windows shape pinned without a window: the command line's quoted-exe + `--startup` spelling (unconditional quoting, the argument owner shared by writer and reader), and the flag parse's case-insensitive presence rule
- `RegistryAutostartStoreTests`: the real registry adapter round-tripped through a temp HKCU subkey (the `TwitchTokenStore` real-DPAPI precedent): the set/get/delete shapes, the null-delete, and the absent-entry read
- `WindowAutostartTests`: the window's autostart on a live STA window: the checkbox commit writes the entry's command line (the running exe + `--startup`), a second commit rewrites it, and the `--startup` launch shape opens the window minimized with the one-shot latch
- `AppSettingsTests`: the machine-local settings store pinned at its interface: the round-trip, the absent → defaults, the corrupt → defaults + one log line, the atomic save (no `.tmp` residue), and the save creating the directory
- `GlobalHotkeyChordPolicyTests`: the chord vocabulary pinned without the OS: the MOD composition, the case/spelling variants, the function/digit keys, and every refusal (the no-modifier veto, the modifier-only veto, the two-main-key veto, the repeated-modifier veto, the unknown-key veto)
- `GlobalHotkeyPolicyTests`: the registration resolution pinned without the OS: the profile-order first-wins duplicate drop (the later widget logs and stays tap-only), the tripped kill switch vetoing the whole candidate set, and the stable desired list
- `GlobalHotkeyManagerTests`: the registration owner pinned through the injected `HotkeyApi`: the `MOD_NOREPEAT` at the OS boundary, the idempotent refresh no-op, the changed-set diff with stable ids, the foreign-owned untracked cell + its one log line, the fire routing, the zero-handle clear, and the dispose unregistration
- `KeyCaptureEditorTests`: the key-capture editor's WPF mapper pinned (the pure model's rules stay in `KeyCaptureModelTests`): the ChordKeyName mapping (letters, digits, and F1-F24 map to a name; a numpad, modifier, symbol, or the raw System key maps to null, so a capture can never record a chord the vocabulary would refuse or one that spells one key while registering another), the press-key resolution policy (a System press routes to the event's system key, a non-System primary passes through), and the editor glue on a live STA window driven with synthesized key events (the click arms the capture on a focusable box, a modifier-less press is swallowed and stays armed, Escape cancels and is handled, a focus loss cancels, and a box that cannot take focus cancels the armed capture instead of zombie-arming it)
- `KeyCaptureModelTests`: the key-capture editor rules pinned without WPF: the house spelling (Ctrl, Alt, Shift, Win order), the all-four-modifiers composition, the digit key, the no-modifier / modifier-as-main refusals (the capture stays armed), the cancel, the ctor seed, and the static composer
- `WindowNavigatePageTests`: the window's page navigation + global-hotkey wiring on a live STA window: `NavigatePage` moving the active page through the `SetActivePageIndex` gate (an out-of-range step a no-op, never a wrap), the WM_HOTKEY posted to the window's HWND routing through the message loop + hook + manager to the owning widget's fire path (the page flips, the MOD_NOREPEAT registration pin), and the duplicate chord's first-widget-wins + one log line
- `WindowAhkScriptTests`: the AHK spawn policy on a live STA window: the kill-switch veto, the blank-script refusal, the unset-interpreter refusal, the missing-interpreter refusal (each a log line, no spawn), and the success path (the user's settings interpreter + the script path ride the injected spawn seam exactly once, with the launch line)
- `HotkeyApiTests`: the production hotkey binding pinned against the real user32.dll: the production delegates are invoked with the degenerate zero chord (HWND 0, id 0, mod 0, vk 0) and released immediately - the pin is that the binding executes (a wrong entry point throws EntryPointNotFoundException on the first call, the 2026-08-26 crash), and the return value is deliberately unasserted (machine-defined; true on this machine)
- `PInvokeBindingTests`: the P/Invoke binding pin (ADR-0020): every src `[DllImport]` spells its `EntryPoint` (the `DebtGuardTests` shape rule), and each spelled (dll, entry point) probes a real export in the real DLL (`GetModuleHandleW`/`LoadLibraryW` + `GetProcAddress`, an export-table lookup that never calls the imported function); the positive/negative controls pin the probe itself (a known export resolves, a forged name does not), an injected synthetic violation pins the shared extractor, and the one judgment call in the spelling pass is recorded (the bare `GetWindowText` method name resolves to the W export under `CharSet.Unicode` - full titles in the production frame-time log - so the explicit spelling is `GetWindowTextW`)
- `CloseBudgetPolicyTests`: the close-budget derivation pinned without the transport: the designed caps dominate for realistic bounds, the half-bound fallback once the bound is below twice the cap, the strict-shorter-than-bound sweep, and the engine/transport same-value agreement
- `MediaDecoderTests`: the media-decode module pinned against binary GIF fixtures: the delta composite over the previous frame (the CopyTo-before-GetPixels order), full-frame replacement, the single-frame still routing, the bomb caps' refusals, and the never-throw contract with partial disposal
- `CaptureWindowGuardTests`: the named ADR-0006 capture window pinned without a fetch: the start-key identity, the live re-read, the `Dropped`/`StillCurrent` verdicts, and the predicate routing through `WeatherQueryKey.SameKey`
- `LhmSharedMemoryReaderTests`: LHS map parsing (JSON + MessagePack index), unit table, malformed-input fallbacks
- `TelemetryStoreMappingTests`: DTO→store mapping, freshness tracking, null-DTO handling
- `TwitchTokenStoreTests`: DPAPI round-trip, overwrite, delete isolation
- `ThemeManagerTests`: WPF resource application (STA thread), lazy theme load
- `ThemeSettingsTests`: hex color parsing, JSON round-trip, the display-copy coverage (every themeable property has a `ThemePresentation` label/description/group, the serializable model staying display-free), the zinc-700 shared-default pin (the Border/M3PrimaryContainer/ControlHover trio follows Border), and the ADR-0021 location/migration pins (the state-dir path's profile-state-dir lockstep, the save's directory creation + round-trip, `LoadFrom`'s absent/corrupt degradation, the full load matrix including the one-time legacy migration's write + log-line pins, the corrupt-target no-repair rule, the same-path guard, the migration write-failure in-memory-copy pin, the pre-read size cap's over-cap degradation (state + legacy + `LoadFrom`), and the present-but-JSON-`null` state file's one-line observability)
- `ProfileExportThemeTests`: the export bundle's optional theme section pinned at its module interface (the literal `"theme"` key placement, the full-property round-trip through `ThemeApplicator.Fingerprint`, the profile's own fields kept at the root, the bare/null-valued/unshaped/corrupt read degradations, the unparseable-export passthrough, and the `ExportJson`-stays-bare pin: the persisted profile never emits the section)
- `ThemeDraftTests`: the theme dialog's draft module pinned at its interface without a window: the seeded entries (the active theme's values, the group-then-name display order, the presentation copy), the single validity verdict (an invalid hex invalidates and names the entry, a valid follow-up revalidates), the apply's parse-only write (an invalid value never reaches the theme), and the reset to defaults
- `ThemeRestorePolicyTests`: the export bundle's theme-restore offer rule pinned at its interface without a window: the null-bundle veto, the same-fingerprint veto (a default-theme export never prompts a default-themed machine), and the differing-fingerprint offer
- `PriceFeedManagerLifecycleTests`: subscription/unsubscription lifecycle, the GetPrice seam, and the one-shot seed's validation guards (polluted/overlong/empty symbols never reach the wire)
- `PriceFeedSocketLoopTests`: WebSocket-seam loop behavior (ticker apply, reconnect) through the feed seam (the CoinGecko table invariant moved to `SymbolCatalogTests`)
- `PriceRestLegTests`: the REST quote legs through the HttpClient seam (the four named source legs' URL shapes + label pins, validation guards, unparseable-response rejection, the CoinGecko leg's id resolution and distinct-id batching against the SymbolCatalog table)
- `PriceMapStoreTests`: the price-map store seam pinned at its interface without the manager or HTTP: the live rule (always overwrites price/timestamp at any age, a sample with no change figure keeps the previously known change, a first write with no change is zero), the fallback rule (a fresh record from any other source is never downgraded, a stale one is replaced, a same-source refresh replaces, a null change keeps the previous change), the `ShouldKeepExisting` guard's pure pins (moved from the manager), and the read/release seams
- `RestPollLoopTests`: the REST poll cycle through its injected delay delegate: delay-first cadence, per-symbol failure isolation, batch-tail ordering, and the live membership view (a symbol added between cycles is polled on the next, a released one stops) on a fake clock
- `DisplayHidTransportTests`: transport policy (init sequence, including the blank-frame bulk write folded into the init verdict, frame framing, touch parsing) through the ITransferBackend seam, and the CloseBound teardown-worst-case pin
- `FramePumpTests`: 30 FPS cadence wiring on a live STA/Dispatcher
- `DeviceTouchDrainTests`: the window's device-touch drain on a live STA window: the queue feeds the gesture machine IN ORDER, an in-order swipe burst (contact → intermediate point → release) switches the page, a release without its contact sample navigates nowhere, and one drain feeds every event of a two-gesture burst
- `TwitchChatConnectionTests`: the chat connection module driven through the `IWebSocketFeed` seam: the handshake payload (CAP/PASS/NICK/JOIN), the CRLF-injection and over-length channel guards (both fall back to the default channel), the connect-fault reconnect recovery, the PING→PONG echo with the same payload, the NOTICE state transitions (login-failed and live, asserted from the recorded transition because the fake's queue drains into the reconnect backoff before a poll can sample), the cycle-end reconnecting state, the stop veto, the buffer clamp over 15 lines with the tail pin, and the restart buffer-clear plus re-join
- `TwitchSessionTests`: the session lifecycle (restore/login/logout/refresh through the injected token store + fake client) and the validation tick's verdicts: the restamp/refresh/clear paths, the hung-validation gate pin (the tick's network call holds no gate, the user's logout completes while the validation is parked), the stale-verdict-after-logout pin (a late verdict must not resurrect a logged-out token), the device flow's trusted-open seam (the test ctor binds a recorder: the verification page opens exactly once at the device's URI, and a lookalike-host URL is refused with a log line and never reaches the open), and the one refresh-verdict routine's pins (the user path's and the tick's refresh routes share the spend → validate → stamp sequence, and the 400/401 rejection's one log line through the context error seam, pinned at both entry points)
- `FontAndTextTests` / `GriddyIconsTests` / `HotkeyActionTests`: subject files own their tests (widget tests live with their widgets); the old UnitTestSuite grab-bag is retired and its residual groups were folded into their subject files, and the per-widget colored-render smoke tests live in their widgets' test files (the `WidgetRenderSmokeTests` grab-bag was likewise retired)
- `FontCacheEvictionTests`: the font cache overflow-bound rule (the strict greater-than boundary and the whole-cache reset eviction) pinned without pixels
- `FramePipelineAllocationTests`: the steady-state render-tick allocation budget pinned on a live STA/Dispatcher: after a warm-up window, a full compose (edit mode on, one selected digital clock, real `SkiaRgb565Encoder`, inert send) measured over a second window must stay under both a per-tick and a total byte budget. Allocations are measured with the per-thread counter (`GC.GetAllocatedBytesForCurrentThread`) because the process-wide `GC.GetTotalAllocatedBytes` quantizes small per-tick allocations into ~8.2 KB chunks on this platform (a sampling artifact, not a real object), which would make the budget unassertable; the per-thread counter is exact for the single composing thread
- `NowPlayingLayoutTests` / `AudioFrameBufferTests` / `FeedSubscriptionTests`: the widget module seams (touch-zone precedence, the buffer gate + double-buffer, feed-identity bookkeeping) pinned without a widget instance
- `AudioCaptureLifecycleTests`: the visualizer's capture-lifecycle module at its interface (the start-on-render / stop-on-stale policy, the fresh-capture watchdog prime, the stale-render deferred stop on a fake clock, the page-switch-back re-arm, the buffer feed, and the start-failure disposal + retry) pinned through the `IAudioCaptureSource` seam without a widget, a canvas, or audio hardware
- `IconPickerModelTests` / `DeviceAuthorizationModelTests`: the dialog decision models pinned without a window (the picker's filter / selection / chip / accept rules over the real Griddy catalog, and the auth model's trusted-open refusal, the copy seam, and the slot-clear identity rule on STA Window handles)
- `ModernWidgetBaseTests`: the property commit owner pinned at its interface (the commit sets the instance, raises the change, and persists into the placed instance's `PropertyValues` through the context; the base's `SetProperty` routes through it with a persisting context, the pre-initialization leg still sets the instance with no context handed, and a missing property writes nothing and raises no change)
- `MediaSessionMonitorTests` / `NowPlayingWidgetLiveSessionTests`: the media-session seam through the `IMediaSessionSource` stub (the neutral status/repeat vocabulary the WinRT adapter maps to once, the version-token-guarded refresh, the session cycling, the raw-command routing) and the tap-action policy (the live-state toggles, the capability vetoes, the repeat step, the seek ratio against the live duration, and the widget-level not-shuffleable veto through the monitor-factory seam)
- `PageTabVisualTests`: the per-tab geometry rules (padding/margins from active + delete state) with no UI tree
- `LocationSearchModelTests`: the Location-search editor rules (seed suffix, query/commit base-label rule, focus-loss veto, the version-token stale rule, the compact population format) pinned without WPF
- `PageTabsViewTests`: the tab-strip module through its real Panel/ScrollViewer on STA: tab/rename/delete seams fire with the right index, the close button obeys the delete rule, the wheel maps to an inverted horizontal offset, and ScrollToPage brings the tab into view
- `PictureSourcePolicyTests`: the picture widget's resolution table (mode × file/folder existence → source), the CanCycle truth, and the hint that follows it, pinned without the filesystem
- `SvgIconHelperTests`: the draw-scaling protocol pinned on pixels (placement, scale, offsets, no-op guards) and the parse cache (empty-path fallback, case-insensitive keying, parse-once)
- `DiagLogTests`: the category-tagged log line's composition rule (first-log/every-Nth + tag) through the injected write seam
- `WinUsbBulkDeviceTests`: the WinUSB/SetupAPI open and cleanup paths through the delegate-bag seam (`WinUsbApi`)
- `SystemTelemetryPresentationTests`: the hardware monitor's display rules (placeholder states, override resolution, the mode fallback, value format clamping, maximum resolution, progress clamp) pinned without pixels
- `SymbolCatalogTests`: symbol/FX validation and normalization, alias resolution, asset-kind mapping, and the single crypto table's CoinGecko invariant (moved out of the feed-lifecycle tests)
- `TwitchChatPresentationTests`: the header status line, empty-state hint, and status color per `ChatStatus`
- `UpdateFlowTests`: the update button's pure flow state machine (the check/download/failure transitions, the one click decision, the pending-update hand-off, and the single tooltip spellings) pinned without WPF or network
- `WeatherDisplayStateTests`: the widget's gated display-state module pinned at its interface without a widget instance, a fetch, or an HTTP stub: the apply's critical section (guard → merge → identity copies → stamp; a rejected apply re-stamps nothing), the tie apply (the placeholder reset + the tied candidates + the queried-name header, the blank query takes the neutral label), the write-back queue/take serialization (a queued value can never be lost to a take; a never-queued take returns null), the write-back take's veto re-decision (a CustomLabel or a name now equal to the location landing after the queue refuses AND keeps the value queued, the `WritebackEligible` policy spelled once and re-evaluated under the gate at the take, a "not yet", never a "never"), the folded-in resolved-identity drop pins (the `WeatherResolutionState` value dropped per `WeatherInvalidationKind` through the gate, the deleted `WeatherResolvedIdentity` module's former test home), the invalidation routing through the gate, the render tick's version-gated consistent view (the forecast copies are stable across captures until the version turns), and the tie reset's forecast-version monotonicity (the no-capture collision, a re-apply after a tie must refresh the copies, never the previous city's list, plus the tie-capture placeholder refresh) and the data fact's rides (the apply commits `HasData`, the tie reset lands on the no-data view, and the render-view's key + inputs carry the flag, `CaptureRenderView_TheDataFact_RidesTheKeyAndInputs`)
- `WeatherFetchFlowTests`: the weather fetch-flow module pinned at its interface without a widget instance, an HTTP mock beyond the shared stub, a render tick, or the widget's gate: the applied path, the single drop gate (the post-await live re-check against the live location, an identity change the client's own window cannot see, verified for `Fetched` and the tie's `Tie` outcome, ADR-0009) routing the forced re-fetch to the NEW identity, the drop-and-refetch tie resolution (the forced re-fetch of a dropped result lands on a tie and applies through the tie seam, the ADR-0009 escape route end-to-end), the tied-outcome apply (the tied candidates populate the pick list, the data state resets to its placeholder with a version bump, the queried name becomes the header, the throttle still stamps), the label write-back gating (city resolution queues the header's own label; a CustomLabel vetoes the write-back; a tie writes no label back, there is no resolved city; the queue-side policy, `WeatherDisplayState.WritebackEligible`, re-decided at the take is pinned in `WeatherDisplayStateTests`), the cadence gate (the client's throttle window, force eligibility, the static-snapshot veto once a fetch stamp exists), and the boot cache load (matching identity + version applies, a version bump during the load prevents the overwrite, an identity change skips the cache AND rolls the client's committed resolution state back through `Invalidate(kind)`, missing/cancelled loads are no-ops)
- `WeatherRenderModelFactoryTests`: the render-model build module pinned at its interface: the cache hit's reference identity (the per-frame path allocates nothing), the every-key-field drift → rebuild rule, the key/data → model mapping, the display composition through the presentation (field-by-field, the record's reference equality on its collection fields makes a whole-record `AreEqual` unrepresentable), the unit-system hero rule, the header's custom-label precedence + ellipsis truncation, the per-metric pill-width measurement, and the location-emptiness key fact (a location edit with no data-version bump rebuilds and flips the subtitle guidance, `Resolve_LocationEmptinessDrift`), and the no-data view's build (the `HasData` key fact's drift forces a rebuild and the model composes the named no-data display, `Resolve_NoData_ComposesTheNoDataDisplay` / `Resolve_HasDataDrift_RebuildsAndSwapsTheView`)
- the weather night-icon pins: the `MapWmoIcon` day/night display fact (the one `(icon, description)` spelling: the moon flip, the night city, precipitation unchanged, the day-neutral description passthrough, and the 0–99 night sweep, `WeatherPresentationTests`), the `is_day` parse rule (modern + legacy shape, both polarities, absent → unknown, `WeatherForecastParserTests`), the same fact read client-side through `FetchCurrentAsync` (polarity per raw `is_day` value, absent → unknown, `WeatherClientTests`), the apply-policy's null-keep for the day/night flag + the state's day-default placeholder (`WeatherSnapshotApplyPolicyTests`), the disk-cache round-trip (a saved night stays a night, a legacy cache reads as unknown → day default, `WeatherCacheStoreTests`), and the model wiring (the flag rides the data view alongside the code and, like the code, the data version, `WeatherRenderModelFactoryTests`)
- `WeatherInvalidationTests`: the twin-equivalence pin: both invalidation twins (the client's `WeatherFetchControl.Invalidate(kind)` and the widget's `WeatherDisplayState.Invalidate(kind)`, the module the deleted `WeatherResolvedIdentity` folded into) driven through every `WeatherInvalidationKind`, both twins' resulting identity values (`WeatherResolutionState`) compared structurally, plus the pure `Drop` pins per kind, so a new resolved field added to the shared record fails the pin the moment a twin's transition forgets to route it through the rule
- `WeatherLocationResolverTests`: the geocoding decision policy pinned without pixels: the abbreviation/state-code-collision tiers, the all-or-nothing suffix rule, the population tiebreak + the ambiguity gate (a bare-name tie refuses coordinates), the postal-routing rules (`TryPostalRoute` battery, numeric/hyphenated/alphanumeric shapes, the US-default route, the `NormalizePostalLookup` indexed shapes), the exact-name predicate + the persisted-pick exact-name promotion (a fuzzy-row pick is NOT promoted, the ranking re-decides for the city the query typed, the wrong-city-on-restart defense), and the `count=100` search pin
- `WeatherGeocoderResolveTests`: the geocoder's resolution door through a stubbed HttpClient: the coordinate/pair/postal/pick/city routing ladder, the postal fallback chain (a zippopotam 404 drops to the geocoder's own postal search, hinted first, then bare; a leg that answers stops the sequence), the pick list's exact-name filter (fuzzy rows absent from the dropdown; postal queries keep every candidate; a tie's candidates refresh the pick list), the zero-HTTP warm-pick fast path + the stale-pick fall-through to the city leg, and the client-level pick pins (a pick resolves from the cached candidates without re-geocoding; a no-longer-offered pick label re-geocodes)
- Shared test doubles in `TestDoubles.cs`: one `TestContext` / `FakeFeed` (queued messages, parkable connect) / `StubHttpHandler` (delegate, canned-body, queue, and gate modes) / scriptable `StubPresentMonNative` / `StubMediaSessionSource` triple / scriptable `StubLhmMapSource` / `RecordingBackend` (the transport-policy seam) / `StaHost` (App-hosting STA pump) / `StaRunner` (one-shot STA invoke, no App host) / `TestWait` (async polling until a condition holds, the house's replacement for Thread.Sleep loops; `WaitForApplied` waits for the widget's post-await apply, not the producer's completion counter, the counter-race rule spelled once in the seam) / `WpfWindow.ShowOwner` (the WPF shown-owner rule for the dialog tests, the owner's Show with one spelling of the requirement) / `ResetApplicationState` (clears the shutdown flag a closed window's Application leaves set, which would silently disable later `Window.Show`s) per seam instead of a per-file copy, plus `FakeTimeProvider` (Microsoft.Extensions.Time.Testing) for clock-dependent tests
- Memory footprint baseline (measured 2026-08-21 on the live process: 15-min 30-s sampling soak + an elevated `dotnet-dump` heap dump analyzed with SOS `eeheap`/`gcheapstat`/`dumpheap -stat`/`gchandles`): steady-state ~440 MB working set / ~335 MB private after warmup, ~550/350 after a heavy-use hour, flat, GC sawtooth only, no drift. Of the private bytes the managed GC heap is ~32 MB (11 MB LOH ≈ the frame pool + telemetry buffers) and the CLR's total is ~53 MB; the frame pipeline, the bounded caches, and the `SKElement` interop (one reused `WriteableBitmap`, per-paint surfaces disposed) verified allocation-clean, so the remaining ~280 MB is the WPF/Direct3D/NVIDIA-driver/Skia/NAudio/WinRT native floor at 1680×900 @ 96 DPI, not reducible from app code without changing the render stack. The App csproj already carries the memory-conservation runtime tuning (`System.GC.HighMemoryPercent=30`, `System.GC.RegionSize=1MB`)
- Line coverage baseline (measured 2026-08-26 with the coverlet XPlat collector over the full Release run through `scripts\measure-coverage.ps1`, 1,973 tests; 33 tests landed after that run (the audit-fix and page-background-picker commits plus the ADR-0021 theme work, 2,006 at the 2026-08-27 run); the prior 2026-08-25 baseline of 1,813 tests predates the manager-parity + P/Invoke-pin work, ADR-0019/0020, that landed since): 87.9% of the 14,263 instrumented src lines covered, per project Sdk 92.9% (526/566), Widgets 92.0% (6,980/7,586), Hardware 89.5% (739/826), Core 85.8% (665/775), App 80.5% (3,632/4,510). The number is the regression floor for future test work; rerun the same command when a large test change lands (the report itself is transient: the script keeps the run log and the cobertura XML under its temp results dir, `C:\Users\tobia\AppData\Local\Temp\opencode\wmd-coverage-results\`)

## Architecture Decisions

| ADR | Decision | Rationale |
|-----|----------|-----------|
| [ADR-0001](docs/adr/0001-synchronous-transport-interface.md) | Synchronous transport interface | USB I/O is inherently blocking; fake async adds cognitive overhead and prevents compile-time detection of sync-over-async |
| [ADR-0002](docs/adr/0002-named-pipe-wcf-transport.md) | ~~Named-pipe WCF transport~~ **Superseded by ADR-0005** | The WCF pipe is gone with the Service (ADR-0005); kept for history |
| [ADR-0003](docs/adr/0003-presentmon-service-for-frame-time.md) | PresentMon Service as frame-time source | Replaces the in-house ETW reader; Intel's LocalSystem service + named-pipe client matches the non-elevation goal with no custom ETW capture to maintain |
| [ADR-0004](docs/adr/0004-librehardwareservice-shared-memory-for-sensors.md) | LibreHardwareService shared memory as hardware sensor source | Replaces the in-house WCF sensor reader; LHS's LocalSystem service + mutex-guarded shared-memory maps match the non-elevation and low-overhead goals with no custom hardware-polling code to maintain |
| [ADR-0005](docs/adr/0005-remove-windows-service.md) | Remove the ModernWigiDash Windows Service | Once telemetry came from PresentMon/LibreHardwareService and the app owned the USB device directly, the Service and its WCF pipe were dead weight, a whole project, a named-pipe attack surface, and install machinery to maintain |
| [ADR-0006](docs/adr/0006-stale-identity-invariant.md) | One owner of the weather stale-identity invariant (`WeatherQueryKey`) | The rule "never apply weather resolved for a different place than the UI shows" was spelled 4+ ways (client capture window, cache-load stamp compare, widget return→apply re-check) and held by comments alone with no test surface. `WeatherQueryKey.Build` is the only key construction and `SameKey` the only predicate, so every guard site routes through one owner with a direct test surface |
| [ADR-0007](docs/adr/0007-weather-fetch-flow-module.md) | One fetch-flow module owns the weather fetch sequence (`WeatherFetchFlow`) | The capture window, the post-await drop gate, the drop-and-refetch routing, the write-back gating, the cadence gate, and the boot-load rollback were spelled across five widget methods, held by comments, and testable only through a live widget; the module enforces the sequence in code and exposes it as one interface (`CanFetch` / `RunFetchAsync` / `RunBootLoadAsync`) the widget forwards to, so the ADR-0006 predicate absorbs the old widget's second inline identity compare and the whole flow is pinned without a widget, a render tick, or the widget's gate |
| [ADR-0008](docs/adr/0008-typed-weather-fetch-host-seam.md) | The weather fetch flow's host seam is typed (`IWeatherFetchHost` + `WeatherApplyRequest`) | ADR-0007's own Negative, the ten-delegate bag was a discipline, not a type: a named seam gives the host contract a single spelling (and a test-double surface) and the apply payload a name, so a new payload field is a named addition to the record instead of a seven-positional-argument signature change on both sides |
| [ADR-0009](docs/adr/0009-weather-tie-outcome-carries-candidates.md) | The weather tie outcome carries its candidates (`WeatherFetchResult.Tie` + `AppliedTie` + `TryApplyTie`) | The ambiguity gate is load-bearing, a bare "Berlin" must never guess a city, but the tie's verdict collapsed into bare `Failed`, so the widget's Location Match dropdown (the documented escape route) was EMPTY exactly when it was needed: the candidates sat in the client twin where no display could reach them. A first-class `Tie` arm (no snapshot; candidates + the ADR-0006 query key) makes the tie assertable at every module boundary and populates the pick list on the first fetch; the escape route then rides the existing zero-HTTP pick path. The presentation follow-up was resolved 2026-08-30: the pane owns a no-data view (the snapshot state's `HasData` fact, riding the render-model key, gates the hero block and the forecast strips, so the placeholder scalars never render as weather; the tied and never-fetched states draw the em dash glyph under the header's guidance line) |
| [ADR-0010](docs/adr/0010-format-gate-does-not-police-line-endings.md) | The format gate does not police line endings (no `end_of_line` pin in `.editorconfig`) | `.editorconfig` pinning `lf` and `.gitattributes`' `text=auto` CRLF checkout contradicted each other: the gate failed with ~45,000 ENDOFLINE errors on every clean Windows checkout, and the noise masked 15 real violations. Line-ending discipline stays with git (`text=auto` normalizes to LF at commit); the gate measures only what git does not police: whitespace, final newlines, charset. Do not "fix" the missing pin back in |
| [ADR-0011](docs/adr/0011-static-telemetry-stores-for-reflection-widgets.md) | Reflection-instantiated widgets read telemetry from static stores | Widgets are reflection-constructed with a parameterless ctor and cannot receive injected dependencies, so `LhmSensorStore`/`FrameTimeStore` (facades over `TelemetryStoreFacade<TRecord>`) are the deliberate DI stand-in: producers write `UpdateFromDto`, consumers read only `TryReadFresh`, and the staleness decision lives in the store. Exit: injected per-instance stores the moment the loader gains a dependency path (DI/MEV, a factory attribute, or a ctor parameter) |
| [ADR-0012](docs/adr/0012-frame-pipeline-no-compose-send-overlap.md) | The frame pipeline encodes on the compose thread and does not overlap compose and send | The pump's 30 FPS contract outruns the display's ~55 ms bulk write (~15-18 FPS device ceiling), and a compositor bitmap ring was evaluated and declined (2026-08-24 hot-path triage): it would need ~1.2 MB per slot and the sender would encode a bitmap the canvas already moved on to, breaking the buffer-drawn-is-exactly-the-buffer-sent identity. The encode's dispatcher cost is pinned by the `FrameEncoderPixelTests` timing canary (full frame under 50 ms against the 33 ms cadence). Exit: revisit overlap when the measured write time falls below ~2× the cadence or the canary starts failing |
| [ADR-0013](docs/adr/0013-deliberate-exceptions-are-allow-list-entries.md) | Deliberate DebtGuard exceptions are allow-list entries with reasons and exit conditions | The mechanical debt pins (`DebtGuardTests`) except a small number of sites on purpose (five sync-over-async files, four handle-marker files). Each exception is an allow-list entry with a reason, drift-checked both directions, and this ADR is the umbrella record holding every entry's rationale and exit condition (the PresentMon service DLL is deliberately not freed, issue #383; the engine's close-path waits are the `CloseBudgets` budgets). Exit per entry: the entry retires when the reason stops being true (e.g. ADR-0001 superseded), and a stale or new entry fails the gate until the register is edited |
| [ADR-0014](docs/adr/0014-startup-wiring-null-tolerant-window.md) | The window's pre-module wiring window is `null!`-typed and null-tolerant | `MainWindow`'s wiring-assigned XAML fields are `null!`-typed for the pre-module window (type-visible, unassigned), and the context's module-deref callbacks are null-tolerant, so a pre-module event degrades to a benign no-op instead of the historical startup NRE; `StartupWiringTests` pins the order. Exit: two-phase construction (a pre-wiring facade or a lazy module resolver) when the pre-module surface grows, a startup NRE appears, or a reorder test must assert a no-op instead of an order |
| [ADR-0015](docs/adr/0015-attribute-literal-choice-arrays-are-pinned-by-test.md) | The inspector's choice arrays live in attributes and are pinned to the runtime catalogs by test | C# attributes cannot bind runtime values, so `[WidgetProperty]`'s `Choices` is a compile-time literal while the behavior reads the runtime catalogs (`HotkeyActionCatalog.Entries`, `WeatherLayout.Modes`). The duplication is deliberate and lock-step pinned at the gate (`HotkeyActionAttribute_Options_MatchTheActionCatalog`, `LayoutModeAttribute_Options_MatchTheModeCatalog`): a renamed entry compiles and fails the gate with a coaching line. Exit: drop the literal and the pin when attributes support runtime-initialized properties or the inspector uses the options provider for static lists |
| [ADR-0016](docs/adr/0016-font-cache-eviction-is-a-whole-reset.md) | The font caches evict by whole-cache reset past a declared cap (no LRU) | All five `FontHelper` caches are keyed on content variety (unbounded key space) and are bounded by the shared `FontCacheEviction` rule: strictly over the declared cap, clear the whole cache and refill on demand. No LRU deliberately, because every entry is a pure recompute, so an evicted entry costs one recompute and an LRU would pay bookkeeping on every hot-path access to save recomputes that only happen at overflow. Exit: a per-entry LRU or size budget when overflow becomes observable (a `FramePipelineAllocationTests` budget failure attributed to font recomputes, or an observed recompute storm) |
| [ADR-0017](docs/adr/0017-telemetry-widgets-degrade-when-services-absent.md) | The telemetry widgets degrade gracefully when the external services are absent | An absent LibreHardwareService or PresentMon Service is a display state, not an error: the producers keep polling with deduped error logging and the widgets render a named placeholder ("LibreHardwareService not running", "PresentMon not installed"). No in-process fallback source is bundled: ADR-0003/ADR-0004 retired the in-house capture precisely to stop maintaining it, and a fallback would resurrect that maintenance. Exit: bundle a fallback producer behind the same store seam when the placeholder becomes the user-visible complaint |
| [ADR-0018](docs/adr/0018-close-and-minimize-hide-to-tray.md) | Close and minimize hide to the tray when opted in; only the tray's Quit exits | An accidental X must not cost the session, but a hide must never strand the window or let a second instance steal the USB device: the behavior is a persisted opt-in (Quit stays the default), one predicate with the tray's liveness as a required operand (the N1 fallback), and a per-session single-instance guard that makes a second launch signal the first and exit |
| [ADR-0019](docs/adr/0019-manager-parity-integration.md) | Manager parity: HKCU autostart, global hotkeys with a machine-local kill switch, and user-supplied AHK scripts | The vendor Manager's settings options at parity without a service or an embedded engine: the autostart is a per-user HKCU Run entry (no elevation, the vendor-verified shape), the chord policy and the OS registration boundary sit on injectable seams (the delegate-bag house pattern), the kill switch is machine-local and scoped to the OS-visible surface (the anti-cheat off switch), the AHK interpreter is user-supplied and spawned bare (nothing bundled, no auto-detection, no IPC surface, ADR-0005's spirit kept), and page nav from a hotkey action rides the same SetActivePageIndex gate as a swipe |
| [ADR-0020](docs/adr/0020-pinvoke-bindings-are-pinned-at-the-gate.md) | P/Invoke bindings are pinned at the gate | The 2026-08-26 hotkey crash (EntryPointNotFoundException on every launch, invisible to the fake-injecting tests) was one rename away from every other extern in src: every `[DllImport]` now spells its `EntryPoint` (the DebtGuard shape rule) and each spelled pair probes the real DLL (`PInvokeBindingTests`), so a binding miss fails the gate, not the on-device loop |
| [ADR-0021](docs/adr/0021-theme-file-lives-in-user-state-and-rides-the-export-bundle.md) | The theme file lives in the user state dir and rides the export bundle as an optional item | The exe-dir theme location carried a stale-copy hazard (a `bin\Release` copy overrode every color until deleted) and a Debug-vs-Release split, and it was never actually decided; the rest of the app's state already lives in `%LOCALAPPDATA%\ModernWigiDash` (and the release notes documented the theme there). The one-time legacy migration keeps an upgraded install's colors, the bundle section rides the verified JetBrains/VS Code pattern (optional section, deliberate per-item restore behind a confirm, the profile import never touches the theme file, and `ExportJson` stays bare so a hand-edited profile cannot smuggle in a theme) |
